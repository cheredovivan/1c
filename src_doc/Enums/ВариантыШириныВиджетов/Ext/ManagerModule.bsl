#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ПрограммныйИнтерфейс

// Определяет ширину виджета из варианта ширины.
//
// Параметры:
//  ВариантШирины - ПеречислениеСсылка.ВариантыШириныВиджетов - Вариант ширины виджета.
// 
// Возвращаемое значение:
//  Число - Ширина виджета указанного варианта.
//
Функция ШиринаВиджета(ВариантШирины) Экспорт
	
	Если ВариантШирины = Узкий Тогда
		
		Возврат 10;
		
	ИначеЕсли ВариантШирины = Обычный Тогда
		
		Возврат 23;
		
	ИначеЕсли ВариантШирины = Полуторный Тогда
		
		Возврат 34;
		
	ИначеЕсли ВариантШирины = Широкий Тогда
		
		Возврат 47;
		
	ИначеЕсли ВариантШирины = ОченьШирокий Тогда
		
		Возврат 71;
		
	Иначе
		
		ВызватьИсключение СтрШаблон(
			НСтр("ru = 'Неизвестный вариант ширины виджета %1.'"),
			ВариантШирины);
		
	КонецЕсли;
	
КонецФункции

// Определяет ширину диаграммы из варианта ширины.
//
// Параметры:
//  ВариантШирины - ПеречислениеСсылка.ВариантыШириныВиджетов - Вариант ширины диаграммы.
// 
// Возвращаемое значение:
//  Число - Ширина диаграммы указанного варианта.
//
Функция ШиринаДиаграммы(ВариантШирины) Экспорт
	
	Если ВариантШирины = Узкий Тогда
		
		Возврат 15;
		
	ИначеЕсли ВариантШирины = Обычный Тогда
		
		Возврат 23;
		
	Иначе
		
		ВызватьИсключение СтрШаблон(
			НСтр("ru = 'Неизвестный вариант ширины диаграммы %1.'"),
			ВариантШирины);
		
	КонецЕсли;

	
КонецФункции

#КонецОбласти

#КонецЕсли