#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ОбработчикиСобытий

Процедура ОбработкаПолученияДанныхВыбора(ДанныеВыбора, Параметры, СтандартнаяОбработка)
	
	Если Параметры.Свойство("ДляПравилЗагрузки") И Параметры.ДляПравилЗагрузки Тогда
		
		СтандартнаяОбработка = Ложь;
		
		ДанныеВыбора = Новый СписокЗначений;
		ДанныеВыбора.Добавить(Бумажная);
		ДанныеВыбора.Добавить(Электронная);
		
	КонецЕсли;
	
КонецПроцедуры

Процедура ОбработкаПолученияФормы(ВидФормы, Параметры, ВыбраннаяФорма, ДополнительнаяИнформация, СтандартнаяОбработка)
	
	Если Параметры.Свойство("ДляПравилЗагрузки") И Параметры.ДляПравилЗагрузки Тогда
		
		МассивДоступныхЗначений = Новый Массив;
		МассивДоступныхЗначений.Добавить(Бумажная);
		МассивДоступныхЗначений.Добавить(Электронная);
		Параметры.Отбор.Вставить("Ссылка", МассивДоступныхЗначений);
		
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#КонецЕсли