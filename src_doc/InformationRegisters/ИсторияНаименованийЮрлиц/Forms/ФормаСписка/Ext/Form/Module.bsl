// @strict-types

#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	МультиязычностьСервер.ПриСозданииНаСервере(ЭтотОбъект);
	КоманднаяПанель.Видимость = Ложь; 
	Подсказка = НСтр("ru = 'Добавление и редактирование актуальных наименований юрлиц доступно только из карточки юрлица'");
	Элементы.ДекорацияПодсказка.Заголовок = Подсказка;

КонецПроцедуры

#КонецОбласти