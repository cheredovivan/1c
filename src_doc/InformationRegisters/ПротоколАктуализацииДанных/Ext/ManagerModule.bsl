
// Добавляет запись в протокол актуализации данных.
// 
// Параметры: Параметры Параметры
//  Период - Дата.
//  АктуализируемыйОбъект - ОпределяемыйТип.АктуализируемыйОбъект.
//  ПравилоАктуализации - ПеречислениеСсылка.ПравилаАктуализации.
//  Комментарий - Строка.
// 
// Возвращаемое значение:
//  
Процедура Записать(Период, АктуализируемыйОбъект, ПравилоАктуализации, Комментарий) Экспорт
	
	НаборЗаписей = СоздатьНаборЗаписей();
	НаборЗаписей.Отбор.Период.Установить(Период);
	НаборЗаписей.Отбор.АктуализируемыйОбъект.Установить(АктуализируемыйОбъект);
	НаборЗаписей.Отбор.ПравилоАктуализации.Установить(ПравилоАктуализации);
	
	НоваяЗапись = НаборЗаписей.Добавить();
	НоваяЗапись.Период = Период;
	НоваяЗапись.АктуализируемыйОбъект = АктуализируемыйОбъект;
	НоваяЗапись.ПравилоАктуализации = ПравилоАктуализации;
	НоваяЗапись.Комментарий = Комментарий;
	
	НаборЗаписей.Записать();
	
КонецПроцедуры