#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура СброситьПопытки(Команда)
	
	СброситьПопыткиНаСервере();
	Элементы.Список.Обновить();
	
КонецПроцедуры

&НаКлиенте
Процедура Обработать(Команда)
	
	ОбработатьНаСервере();
	Элементы.Список.Обновить();
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

&НаСервере
Процедура СброситьПопыткиНаСервере()
	
	ОбновлениеКэширующихДанных.СброситьПопытки();
	
КонецПроцедуры

&НаСервере
Процедура ОбработатьНаСервере()
	
	ОбновлениеКэширующихДанных.ОбновлениеКэширующихДанных();
	
КонецПроцедуры

#КонецОбласти