#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ПрограммныйИнтерфейс

// Формирует данные всех команд.
// 
// Возвращаемое значение:
// 	Соответствие из КлючИЗначение - Данные команд:
// 	 * Ключ - СправочникСсылка.КомандыВиджетов.
// 	 * Значение - Структура - Данные команды. См. Справочники.КомандыВиджетов.НовыйДанныеКоманды.
//
Функция ДанныеВсехКоманд() Экспорт
	
	Возврат ДанныеКоманд();
	
КонецФункции

// Формирует данные команд.
//
// Параметры:
//  Команда - СправочникСсылка.КомандыВиджетов.
// 
// Возвращаемое значение:
//  Соответствие из КлючИЗначение - Данные команд:
//   * Ключ - СправочникСсылка.КомандыВиджетов.
//   * Значение - Структура - Данные команды. См. Справочники.КомандыВиджетов.НовыйДанныеКоманды.
//
Функция ДанныеКоманд(КомандыВиджетов = Неопределено) Экспорт
	
	ДанныеВсехКоманд = Новый Соответствие;
	
	Запрос = Новый Запрос(
		"ВЫБРАТЬ
		|	КомандыВиджетов.ВерсияДанных,
		|	КомандыВиджетов.Действие,
		|	КомандыВиджетов.Заголовок,
		|	КомандыВиджетов.ИмяПредопределенныхДанных,
		|	КомандыВиджетов.Источник,
		|	КомандыВиджетов.ИсточникСтрока,
		|	КомандыВиджетов.Картинка,
		|	КомандыВиджетов.Наименование,
		|	КомандыВиджетов.НаименованиеЯзык1,
		|	КомандыВиджетов.ПараметрДействия1,
		|	КомандыВиджетов.ПараметрДействия2,
		|	КомандыВиджетов.Подсказка,
		|	КомандыВиджетов.ПометкаУдаления,
		|	КомандыВиджетов.Предопределенный,
		|	КомандыВиджетов.Представление,
		|	КомандыВиджетов.Ссылка,
		|	КомандыВиджетов.Условие,
		|	ВЫБОР
		|		КОГДА ТИПЗНАЧЕНИЯ(КомандыВиджетов.Источник) = ТИП(Справочник.Реестры)
		|			ТОГДА ПРЕДСТАВЛЕНИЕ(ВЫРАЗИТЬ(КомандыВиджетов.Источник КАК Справочник.Реестры))
		|		ИНАЧЕ """"
		|	КОНЕЦ КАК ПредставлениеРеестра
		|ИЗ
		|	Справочник.КомандыВиджетов КАК КомандыВиджетов
		|ГДЕ
		|	КомандыВиджетов.Ссылка В (&КомандыВиджетов)
		|	И КомандыВиджетов.ПометкаУдаления = ЛОЖЬ");
	
	Если КомандыВиджетов <> Неопределено Тогда
		Запрос.УстановитьПараметр("КомандыВиджетов", КомандыВиджетов);
	Иначе
		Запрос.Текст = СтрЗаменить(Запрос.Текст, "КомандыВиджетов.Ссылка В (&КомандыВиджетов)", "");
		Запрос.Текст = СтрЗаменить(Запрос.Текст, "И КомандыВиджетов.ПометкаУдаления = ЛОЖЬ", "КомандыВиджетов.ПометкаУдаления = ЛОЖЬ");
	КонецЕсли;
	
	Выборка = Запрос.Выполнить().Выбрать();
	Пока Выборка.Следующий() Цикл
		
		Попытка
			
			ДанныеКоманды = СформироватьДанныеКоманды(Выборка);
			
		Исключение
			
			ИнформацияОбОшибке = ИнформацияОбОшибке();
			ТекстЖурналаРегистрации = ПодробноеПредставлениеОшибки(ИнформацияОбОшибке);
			
			СобытиеЖурналаРегистрации = РаботаСВиджетами.СобытиеЖурналаРегистрации();
			
			ЗаписьЖурналаРегистрации(
				СобытиеЖурналаРегистрации,
				УровеньЖурналаРегистрации.Ошибка,,,
				ТекстЖурналаРегистрации);
			
			Продолжить;
			
		КонецПопытки;
		
		Если Не КомандаДоступна(ДанныеКоманды) Тогда
			Продолжить;
		КонецЕсли;
		
		ИнициализироватьКоманду(ДанныеКоманды);
	
		ДанныеВсехКоманд[Выборка.Ссылка] = ДанныеКоманды;
		
	КонецЦикла;
	
	Возврат ДанныеВсехКоманд;
	
КонецФункции

// Данные команды виджета.
// 
// Параметры:
//  Команда - СправочникСсылка.КомандыВиджетов.
// 
// Возвращаемое значение:
//  Структура - Данные команды. См. Справочники.КомандыВиджетов.НовыйДанныеКоманды.
//
Функция ДанныеКоманды(Команда) Экспорт
	
	КомандыВиджетов = ОбщегоНазначенияКлиентСервер.ЗначениеВМассиве(Команда);
	ДанныеКоманд = ДанныеКоманд(КомандыВиджетов);
	ДанныеКоманды = ДанныеКоманд[Команда];
	
	Возврат ДанныеКоманды;
	
КонецФункции

// Данные команды "Еще".
// 
// Параметры:
//  ДанныеКомандМеню - Массив.
// 
// Возвращаемое значение:
//  Структура, Неопределено - Данные команды "Еще". См. Справочники.КомандыВиджетов.НовыйДанныеКоманды.
//
Функция ДанныеКомандыЕще(ДанныеКомандМеню) Экспорт
	
	Если ДанныеКомандМеню.Количество() = 0 Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	ДанныеКоманды = НовыйДанныеКоманды();
	ДанныеКоманды.Заголовок = НСтр("ru = 'Еще'");
	ДанныеКоманды.Подсказка = НСтр("ru = 'Показать остальные команды'");
	
	Меню = Новый Массив;
	Для Каждого ДанныеКомандыМеню Из ДанныеКомандМеню Цикл
		ЭлементМеню = Новый Структура;
		ЭлементМеню.Вставить("Заголовок", ДанныеКомандыМеню.Заголовок);
		ЭлементМеню.Вставить("Картинка", ДанныеКомандыМеню.Картинка);
		ЭлементМеню.Вставить("Действие", ДанныеКомандыМеню.Действие);
		Меню.Добавить(ЭлементМеню);
	КонецЦикла;
	ДанныеКоманды.Действие = Перечисления.ДействияВиджетов.ПоказатьВыборИзМеню(Меню);
	
	Возврат ДанныеКоманды;
	
КонецФункции

// Данные команды "Ничего не делать".
// 
// Параметры:
//  Заголовок - Строка.
// 
// Возвращаемое значение:
//  Структура - Данные команды "ничего не делать". См. Справочники.КомандыВиджетов.НовыйДанныеКоманды.
//
Функция ДанныеКомандыНичегоНеДелать(Заголовок) Экспорт
	
	ДанныеКоманды = НовыйДанныеКоманды();
	ДанныеКоманды.Заголовок = Заголовок;
	ДанныеКоманды.Действие = Перечисления.ДействияВиджетов.НичегоНеДелать();
	
	Возврат ДанныеКоманды;
	
КонецФункции

// Заполняет поставляемые данные.
// 
Процедура ЗаполнитьПоставляемыеДанные() Экспорт
	
	// Адресная книга
	КомандаВиджетаОбъект = АдреснаяКнига.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Адресная книга'");
	КомандаВиджетаОбъект.Заголовок = "";
	КомандаВиджетаОбъект.Подсказка = "";
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "Справочник.АдреснаяКнига.Команда.АдреснаяКнига";
	КомандаВиджетаОбъект.Картинка = "";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Алгоритмы проверки
	КомандаВиджетаОбъект = АлгоритмыПроверки.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Алгоритмы проверки'");
	КомандаВиджетаОбъект.Заголовок = "";
	КомандаВиджетаОбъект.Подсказка = "";
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "Справочник.АлгоритмыПроверки";
	КомандаВиджетаОбъект.Картинка = "";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Банки
	КомандаВиджетаОбъект = Банки.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Банки'");
	КомандаВиджетаОбъект.Заголовок = "";
	КомандаВиджетаОбъект.Подсказка = "";
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "Справочник.КлассификаторБанков";
	КомандаВиджетаОбъект.Картинка = "";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Бронирование помещений
	КомандаВиджетаОбъект = БронированиеПомещений.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Бронирование помещений'");
	КомандаВиджетаОбъект.Заголовок = "";
	КомандаВиджетаОбъект.Подсказка = "";
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "Документ.Бронь.Команда.Бронирование";
	КомандаВиджетаОбъект.Картинка = "";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Валюты
	КомандаВиджетаОбъект = Валюты.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Валюты'");
	КомандаВиджетаОбъект.Заголовок = "";
	КомандаВиджетаОбъект.Подсказка = "";
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "Справочник.Валюты";
	КомандаВиджетаОбъект.Картинка = "";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Виды документов
	КомандаВиджетаОбъект = ВидыДокументов.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Виды документов'");
	КомандаВиджетаОбъект.Заголовок = "";
	КомандаВиджетаОбъект.Подсказка = "";
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "ОбщаяКоманда.ВидыДокументов";
	КомандаВиджетаОбъект.Картинка = "";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Виды документов физических лиц
	КомандаВиджетаОбъект = ВидыДокументовФизическихЛиц.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Виды документов физических лиц'");
	КомандаВиджетаОбъект.Заголовок = "";
	КомандаВиджетаОбъект.Подсказка = "";
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "Справочник.ВидыДокументовФизическихЛиц";
	КомандаВиджетаОбъект.Картинка = "";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Виды контактной информации
	КомандаВиджетаОбъект = ВидыКонтактнойИнформации.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Виды контактной информации'");
	КомандаВиджетаОбъект.Заголовок = "";
	КомандаВиджетаОбъект.Подсказка = "";
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "Справочник.ВидыКонтактнойИнформации";
	КомандаВиджетаОбъект.Картинка = "";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Виды мероприятий
	КомандаВиджетаОбъект = ВидыМероприятий.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Виды мероприятий'");
	КомандаВиджетаОбъект.Заголовок = "";
	КомандаВиджетаОбъект.Подсказка = "";
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "Справочник.ВидыМероприятий";
	КомандаВиджетаОбъект.Картинка = "";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Виды отсутствий
	КомандаВиджетаОбъект = ВидыОтсутствий.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Виды отсутствий'");
	КомандаВиджетаОбъект.Заголовок = "";
	КомандаВиджетаОбъект.Подсказка = "";
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "Справочник.ВидыОтсутствий";
	КомандаВиджетаОбъект.Картинка = "";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Виды проектов
	КомандаВиджетаОбъект = ВидыПроектов.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Виды проектов'");
	КомандаВиджетаОбъект.Заголовок = "";
	КомандаВиджетаОбъект.Подсказка = "";
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "Справочник.ВидыПроектов";
	КомандаВиджетаОбъект.Картинка = "";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Виды работ
	КомандаВиджетаОбъект = ВидыРабот.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Виды работ'");
	КомандаВиджетаОбъект.Заголовок = "";
	КомандаВиджетаОбъект.Подсказка = "";
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "Справочник.ВидыРабот";
	КомандаВиджетаОбъект.Картинка = "";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Вопросы деятельности
	КомандаВиджетаОбъект = ВопросыДеятельности.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Вопросы деятельности'");
	КомандаВиджетаОбъект.Заголовок = "";
	КомандаВиджетаОбъект.Подсказка = "";
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "Справочник.ВопросыДеятельности";
	КомандаВиджетаОбъект.Картинка = "";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Все задачи
	КомандаВиджетаОбъект = ВсеЗадачи.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Все задачи'");
	КомандаВиджетаОбъект.Заголовок = "";
	КомандаВиджетаОбъект.Подсказка = "";
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "Документ.Задача.Команда.ВсеЗадачи";
	КомандаВиджетаОбъект.Картинка = "";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Все календари
	КомандаВиджетаОбъект = ВсеКалендари.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Все календари'");
	КомандаВиджетаОбъект.Заголовок = "";
	КомандаВиджетаОбъект.Подсказка = "";
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "Справочник.ЗаписиРабочегоКалендаря.Команда.ОткрытьДругиеКалендари";
	КомандаВиджетаОбъект.Картинка = "";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Все мероприятия
	КомандаВиджетаОбъект = ВсеМероприятия.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Все мероприятия'");
	КомандаВиджетаОбъект.Заголовок = "";
	КомандаВиджетаОбъект.Подсказка = "";
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "Справочник.Мероприятия.Команда.ВсеМероприятия";
	КомандаВиджетаОбъект.Картинка = "";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Графики работы
	КомандаВиджетаОбъект = ГрафикиРаботы.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Графики работы'");
	КомандаВиджетаОбъект.Заголовок = "";
	КомандаВиджетаОбъект.Подсказка = "";
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "Справочник.ГрафикиРаботы";
	КомандаВиджетаОбъект.Картинка = "";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Документы предприятия
	КомандаВиджетаОбъект = ДокументыПредприятия.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Документы'");
	КомандаВиджетаОбъект.Заголовок = "";
	КомандаВиджетаОбъект.Подсказка = "";
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "Справочник.ДокументыПредприятия.Команда.СписокДокументовПредприятия";
	КомандаВиджетаОбъект.Картинка = "";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Документы предприятия (создать)
	КомандаВиджетаОбъект = ДокументыПредприятияСоздать.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Документы (создать)'");
	КомандаВиджетаОбъект.Заголовок = НСтр("ru = 'Создать документ'");
	КомандаВиджетаОбъект.Подсказка = "";
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "Справочник.ДокументыПредприятия.Команда.СоздатьДокументПредприятия";
	КомандаВиджетаОбъект.Картинка = "ВиджетБелыйСоздать";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Должности
	КомандаВиджетаОбъект = Должности.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Должности'");
	КомандаВиджетаОбъект.Заголовок = "";
	КомандаВиджетаОбъект.Подсказка = "";
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "Справочник.Должности";
	КомандаВиджетаОбъект.Картинка = "";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Ежедневные отчеты
	КомандаВиджетаОбъект = ЕжедневныеОтчеты.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Ежедневные отчеты'");
	КомандаВиджетаОбъект.Заголовок = НСтр("ru = 'Ежедневные отчеты'");
	КомандаВиджетаОбъект.Подсказка = НСтр("ru = 'Открыть ежедневные отчеты'");
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "Документ.ЕжедневныйОтчет.Команда.ОткрытьСвоиОтчеты";
	КомандаВиджетаОбъект.Картинка = "";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Журнал передачи
	КомандаВиджетаОбъект = ЖурналПередачи.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Журнал передачи'");
	КомандаВиджетаОбъект.Заголовок = "";
	КомандаВиджетаОбъект.Подсказка = "";
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "РегистрСведений.ЖурналПередачиДокументов.Команда.ОткрытьФормуСписка";
	КомандаВиджетаОбъект.Картинка = "";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Задачи мне
	КомандаВиджетаОбъект = ЗадачиМне.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Задачи мне'");
	КомандаВиджетаОбъект.Заголовок = "";
	КомандаВиджетаОбъект.Подсказка = "";
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "Документ.Задача.Команда.ЗадачиМне";
	КомандаВиджетаОбъект.Картинка = "";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Задачи от меня
	КомандаВиджетаОбъект = ЗадачиОтМеня.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Задачи от меня'");
	КомандаВиджетаОбъект.Заголовок = "";
	КомандаВиджетаОбъект.Подсказка = "";
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "Документ.Задача.Команда.ЗадачиОтМеня";
	КомандаВиджетаОбъект.Картинка = "";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Задачи подчиненных
	КомандаВиджетаОбъект = ЗадачиПодчиненных.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Задачи подчиненных'");
	КомандаВиджетаОбъект.Заголовок = "";
	КомандаВиджетаОбъект.Подсказка = "";
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "Документ.Задача.Команда.ЗадачиПодчиненных";
	КомандаВиджетаОбъект.Картинка = "";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Замещающие
	КомандаВиджетаОбъект = Замещающие.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Замещающие'");
	КомандаВиджетаОбъект.Заголовок = "";
	КомандаВиджетаОбъект.Подсказка = "";
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "Справочник.ЗамещающиеИПомощники.Команда.Замещающие";
	КомандаВиджетаОбъект.Картинка = "";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Замещающие (создать)
	КомандаВиджетаОбъект = ЗамещающиеСоздать.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Замещающие (создать)'");
	КомандаВиджетаОбъект.Заголовок = "";
	КомандаВиджетаОбъект.Подсказка = "";
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "Справочник.ЗамещающиеИПомощники.Команда.ЗамещающиеСоздать";
	КомандаВиджетаОбъект.Картинка = "ВиджетБелыйСоздать";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Индексы нумерации
	КомандаВиджетаОбъект = ИндексыНумерации.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Индексы нумерации'");
	КомандаВиджетаОбъект.Заголовок = "";
	КомандаВиджетаОбъект.Подсказка = "";
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "РегистрСведений.ИндексыНумерации";
	КомандаВиджетаОбъект.Картинка = "";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Инструкции
	КомандаВиджетаОбъект = Инструкции.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Инструкции'");
	КомандаВиджетаОбъект.Заголовок = "";
	КомандаВиджетаОбъект.Подсказка = "";
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "Справочник.Инструкции";
	КомандаВиджетаОбъект.Картинка = "";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Календари
	КомандаВиджетаОбъект = Календари.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Календари'");
	КомандаВиджетаОбъект.Заголовок = "";
	КомандаВиджетаОбъект.Подсказка = "";
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "Справочник.Календари";
	КомандаВиджетаОбъект.Картинка = "";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Контрагенты
	КомандаВиджетаОбъект = Контрагенты.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Контрагенты'");
	КомандаВиджетаОбъект.Заголовок = "";
	КомандаВиджетаОбъект.Подсказка = "";
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "Справочник.Контрагенты";
	КомандаВиджетаОбъект.Картинка = "";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Мероприятия
	КомандаВиджетаОбъект = Мероприятия.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Мероприятия'");
	КомандаВиджетаОбъект.Заголовок = "";
	КомандаВиджетаОбъект.Подсказка = "";
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "Справочник.Мероприятия.Команда.МоиМероприятия";
	КомандаВиджетаОбъект.Картинка = "";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Мероприятия (создать)
	КомандаВиджетаОбъект = МероприятияСоздать.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Мероприятия (создать)'");
	КомандаВиджетаОбъект.Заголовок = НСтр("ru = 'Создать мероприятие'");
	КомандаВиджетаОбъект.Подсказка = "";
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "Справочник.Мероприятия.Команда.СоздатьМероприятие";
	КомандаВиджетаОбъект.Картинка = "ВиджетБелыйСоздать";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Места хранения дел
	КомандаВиджетаОбъект = МестаХраненияДел.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Места хранения дел'");
	КомандаВиджетаОбъект.Заголовок = "";
	КомандаВиджетаОбъект.Подсказка = "";
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "Справочник.МестаХраненияДел";
	КомандаВиджетаОбъект.Картинка = "";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Мои документы
	КомандаВиджетаОбъект = ВиджетМоиДокументы.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Мои документы'");
	КомандаВиджетаОбъект.Заголовок = "";
	КомандаВиджетаОбъект.Подсказка = "";
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "РегистрСведений.МоиДокументы.Команда.МоиДокументы";
	КомандаВиджетаОбъект.Картинка = "";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Мои контрольные точки
	КомандаВиджетаОбъект = МоиКонтрольныеТочки.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Мои контрольные точки'");
	КомандаВиджетаОбъект.Заголовок = "";
	КомандаВиджетаОбъект.Подсказка = "";
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "Справочник.КонтрольныеТочки.Команда.МоиКонтрольныеТочки";
	КомандаВиджетаОбъект.Картинка = "";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Мои процессы
	КомандаВиджетаОбъект = МоиПроцессы.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Мои процессы'");
	КомандаВиджетаОбъект.Заголовок = "";
	КомандаВиджетаОбъект.Подсказка = "";
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "ОбщаяКоманда.МоиПроцессы";
	КомандаВиджетаОбъект.Картинка = "";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Мой календарь
	КомандаВиджетаОбъект = МойКалендарь.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Мой календарь'");
	КомандаВиджетаОбъект.Заголовок = "";
	КомандаВиджетаОбъект.Подсказка = "";
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "Справочник.ЗаписиРабочегоКалендаря.Команда.ОткрытьКалендарь";
	КомандаВиджетаОбъект.Картинка = "";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Наименования строн
	КомандаВиджетаОбъект = НаименованияСторон.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Наименования строн'");
	КомандаВиджетаОбъект.Заголовок = "";
	КомандаВиджетаОбъект.Подсказка = "";
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "Справочник.НаименованияСторон";
	КомандаВиджетаОбъект.Картинка = "";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// На контроле
	КомандаВиджетаОбъект = НаКонтроле.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'На контроле'");
	КомандаВиджетаОбъект.Заголовок = "";
	КомандаВиджетаОбъект.Подсказка = "";
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "Справочник.Контроль.Команда.НаКонтроле";
	КомандаВиджетаОбъект.Картинка = "";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Написать письмо
	КомандаВиджетаОбъект = НаписатьПисьмо.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Написать письмо'");
	КомандаВиджетаОбъект.Заголовок = НСтр("ru = 'Написать письмо'");
	КомандаВиджетаОбъект.Подсказка = НСтр("ru = 'Написать новое письмо'");
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "ОбщаяКоманда.СоздатьПисьмо";
	КомандаВиджетаОбъект.Картинка = "ВиджетБелыйСоздать";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Настройки рабочего стола
	КомандаВиджетаОбъект = НастройкиРабочегоСтола.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Настройки рабочего стола'");
	КомандаВиджетаОбъект.Заголовок = "";
	КомандаВиджетаОбъект.Подсказка = "";
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "РегистрСведений.НастройкиРабочихСтолов.Команда.НастройкиРабочегоСтола";
	КомандаВиджетаОбъект.Картинка = "";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Настройки реестров
	КомандаВиджетаОбъект = НастройкиРеестров.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Настройки реестров'");
	КомандаВиджетаОбъект.Заголовок = "";
	КомандаВиджетаОбъект.Подсказка = "";
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "Справочник.Реестры.Команда.НастройкиРеестров";
	КомандаВиджетаОбъект.Картинка = "";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Настройки связей
	КомандаВиджетаОбъект = НастройкиСвязей.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Настройки связей'");
	КомандаВиджетаОбъект.Заголовок = "";
	КомандаВиджетаОбъект.Подсказка = "";
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "РегистрСведений.НастройкаСвязей";
	КомандаВиджетаОбъект.Картинка = "";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Номенклатура
	КомандаВиджетаОбъект = Номенклатура.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Номенклатура'");
	КомандаВиджетаОбъект.Заголовок = "";
	КомандаВиджетаОбъект.Подсказка = "";
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "Справочник.Номенклатура";
	КомандаВиджетаОбъект.Картинка = "";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Номенклатура дел
	КомандаВиджетаОбъект = НоменклатураДел.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Номенклатура дел'");
	КомандаВиджетаОбъект.Заголовок = "";
	КомандаВиджетаОбъект.Подсказка = "";
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "Справочник.НоменклатураДел";
	КомандаВиджетаОбъект.Картинка = "";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Нумераторы
	КомандаВиджетаОбъект = Нумераторы.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Нумераторы'");
	КомандаВиджетаОбъект.Заголовок = "";
	КомандаВиджетаОбъект.Подсказка = "";
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "Справочник.Нумераторы";
	КомандаВиджетаОбъект.Картинка = "";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Обо мне
	КомандаВиджетаОбъект = ОбоМне.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Обо мне'");
	КомандаВиджетаОбъект.Заголовок = "";
	КомандаВиджетаОбъект.Подсказка = "";
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "ОбщаяКоманда.СведенияОПользователе";
	КомандаВиджетаОбъект.Картинка = "";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Локализация
	// Обращения граждан и организаций
	КомандаВиджетаОбъект = ОбращенияГраждан.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование
		= Метаданные.Справочники.ДокументыПредприятия.Команды.ОткрытьОбращенияГраждан.Синоним;
	КомандаВиджетаОбъект.Заголовок = "";
	КомандаВиджетаОбъект.Подсказка = "";
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "Справочник.ДокументыПредприятия.Команда.ОткрытьОбращенияГраждан";
	КомандаВиджетаОбъект.Картинка = "";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	// Локализация Конец
	
	// Организации
	КомандаВиджетаОбъект = Организации.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = РедакцииКонфигурацииКлиентСервер.Организации();
	КомандаВиджетаОбъект.Заголовок = "";
	КомандаВиджетаОбъект.Подсказка = "";
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "Справочник.Организации.Команда.Организации";
	КомандаВиджетаОбъект.Картинка = "";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Отсутствия сотрудников
	КомандаВиджетаОбъект = ОтсутствияСотрудников.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Отсутствия сотрудников'");
	КомандаВиджетаОбъект.Заголовок = "";
	КомандаВиджетаОбъект.Подсказка = "";
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "Документ.Отсутствие.Команда.Отсутствия";
	КомандаВиджетаОбъект.Картинка = "";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Отсутствия сотрудников (создать)
	КомандаВиджетаОбъект = ОтсутствияСотрудниковСоздать.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Отсутствия сотрудников (создать)'");
	КомандаВиджетаОбъект.Заголовок = НСтр("ru = 'Создать отсутствие'");
	КомандаВиджетаОбъект.Подсказка = "";
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "Документ.Отсутствие.Команда.СоздатьОтсутствие";
	КомандаВиджетаОбъект.Картинка = "ВиджетБелыйСоздать";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Отчеты
	КомандаВиджетаОбъект = Отчеты.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Отчеты'");
	КомандаВиджетаОбъект.Заголовок = НСтр("ru = 'Отчеты'");
	КомандаВиджетаОбъект.Подсказка = НСтр("ru = 'Открыть отчеты'");
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "Обработка.ВсеОтчеты";
	КомандаВиджетаОбъект.Картинка = "";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Персональные настройки
	КомандаВиджетаОбъект = ПерсональныеНастройки.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Персональные настройки'");
	КомандаВиджетаОбъект.Заголовок = "";
	КомандаВиджетаОбъект.Подсказка = "";
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "Обработка.ПерсональныеНастройки.Команда.ПерсональныеНастройки";
	КомандаВиджетаОбъект.Картинка = "";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Помощники
	КомандаВиджетаОбъект = Помощники.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Помощники'");
	КомандаВиджетаОбъект.Заголовок = "";
	КомандаВиджетаОбъект.Подсказка = "";
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "Справочник.ЗамещающиеИПомощники.Команда.Помощники";
	КомандаВиджетаОбъект.Картинка = "";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Помощники (создать)
	КомандаВиджетаОбъект = ПомощникиСоздать.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Помощники (создать)'");
	КомандаВиджетаОбъект.Заголовок = "";
	КомандаВиджетаОбъект.Подсказка = "";
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "Справочник.ЗамещающиеИПомощники.Команда.ПомощникиСоздать";
	КомандаВиджетаОбъект.Картинка = "ВиджетБелыйСоздать";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Поставить на контроль
	КомандаВиджетаОбъект = ПоставитьНаКонтроль.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Поставить на контроль'");
	КомандаВиджетаОбъект.Заголовок = НСтр("ru = 'Поставить на контроль'");
	КомандаВиджетаОбъект.Подсказка = НСтр("ru = 'Поставить на контроль'");
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "Справочник.Контроль";
	КомандаВиджетаОбъект.Картинка = "ВиджетБелыйСоздать";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.СоздатьЭлемент;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Почта
	КомандаВиджетаОбъект = ВиджетПочта.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Почта'");
	КомандаВиджетаОбъект.Заголовок = "";
	КомандаВиджетаОбъект.Подсказка = "";
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "ОбщаяКоманда.ЭлектроннаяПочта";
	КомандаВиджетаОбъект.Картинка = "";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Проекты
	КомандаВиджетаОбъект = Проекты.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Проекты'");
	КомандаВиджетаОбъект.Заголовок = "";
	КомандаВиджетаОбъект.Подсказка = "";
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "Справочник.Проекты.Команда.ВсеПроекты";
	КомандаВиджетаОбъект.Картинка = "";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Процессы
	КомандаВиджетаОбъект = Процессы.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Процессы'");
	КомандаВиджетаОбъект.Заголовок = "";
	КомандаВиджетаОбъект.Подсказка = "";
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "ОбщаяКоманда.ОткрытьСписокПроцессов";
	КомандаВиджетаОбъект.Картинка = "";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Рабочие группы
	КомандаВиджетаОбъект = РабочиеГруппы.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Рабочие группы'");
	КомандаВиджетаОбъект.Заголовок = "";
	КомандаВиджетаОбъект.Подсказка = "";
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "Справочник.РабочиеГруппы";
	КомандаВиджетаОбъект.Картинка = "";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Роли исполнителей
	КомандаВиджетаОбъект = РолиИсполнителей.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Роли исполнителей'");
	КомандаВиджетаОбъект.Заголовок = "";
	КомандаВиджетаОбъект.Подсказка = "";
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "Справочник.РолиИсполнителей";
	КомандаВиджетаОбъект.Картинка = "";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Сменить пароль
	КомандаВиджетаОбъект = СменитьПароль.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Сменить пароль'");
	КомандаВиджетаОбъект.Заголовок = "";
	КомандаВиджетаОбъект.Подсказка = "";
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "ОбщаяКоманда.СменитьПароль";
	КомандаВиджетаОбъект.Картинка = "";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Создать задачу
	КомандаВиджетаОбъект = СоздатьЗадачу.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Создать задачу'");
	КомандаВиджетаОбъект.Заголовок = НСтр("ru = 'Создать задачу'");
	КомандаВиджетаОбъект.Подсказка = НСтр("ru = 'Создать новую задачу'");
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "Документ.Задача.Команда.СоздатьЗадачу";
	КомандаВиджетаОбъект.Картинка = "ВиджетБелыйСоздать";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Создать запись календаря
	КомандаВиджетаОбъект = СоздатьЗаписьКалендаря.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Создать запись календаря'");
	КомандаВиджетаОбъект.Заголовок = НСтр("ru = 'Создать запись календаря'");
	КомандаВиджетаОбъект.Подсказка = НСтр("ru = 'Создать новую запись календаря'");
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "Справочник.ЗаписиРабочегоКалендаря.Команда.СоздатьЗаписьКалендаря";
	КомандаВиджетаОбъект.Картинка = "ВиджетБелыйСоздать";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Создать мероприятие
	КомандаВиджетаОбъект = СоздатьМероприятие.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Создать мероприятие'");
	КомандаВиджетаОбъект.Заголовок = НСтр("ru = 'Создать мероприятие'");
	КомандаВиджетаОбъект.Подсказка = НСтр("ru = 'Создать новое мероприятие'");
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "Справочник.Мероприятия.Команда.СоздатьМероприятие";
	КомандаВиджетаОбъект.Картинка = "ВиджетБелыйСоздать";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Создать обращение от гражданина/организации
	КомандаВиджетаОбъект = СоздатьОбращениеГражданина.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Создать документ вида ""обращение гражданина/организации""'");
	КомандаВиджетаОбъект.Заголовок = НСтр("ru = 'Создать документ ""Обращение гражданина/организации""'");
	КомандаВиджетаОбъект.Подсказка = "";
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "Справочник.ДокументыПредприятия.Команда.СоздатьОбращениеГражданина";
	КомандаВиджетаОбъект.Картинка = "ВиджетБелыйСоздать";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Создать тему форума
	КомандаВиджетаОбъект = СоздатьТемуФорума.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Создать тему форума'");
	КомандаВиджетаОбъект.Заголовок = НСтр("ru = 'Создать тему форума'");
	КомандаВиджетаОбъект.Подсказка = НСтр("ru = 'Создать новую тему форума'");
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "Справочник.СообщенияОбсуждений";
	КомандаВиджетаОбъект.Картинка = "ВиджетБелыйСоздать";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.СоздатьЭлемент;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Способы доставки
	КомандаВиджетаОбъект = СпособыДоставки.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Способы доставки'");
	КомандаВиджетаОбъект.Заголовок = "";
	КомандаВиджетаОбъект.Подсказка = "";
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "Справочник.СпособыДоставки";
	КомандаВиджетаОбъект.Картинка = "";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Статьи движения денежных средств
	КомандаВиджетаОбъект = СтатьиДвиженияДенежныхСредств.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Статьи движения денежных средств'");
	КомандаВиджетаОбъект.Заголовок = "";
	КомандаВиджетаОбъект.Подсказка = "";
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "Справочник.СтатьиДвиженияДенежныхСредств";
	КомандаВиджетаОбъект.Картинка = "";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Страны мира
	КомандаВиджетаОбъект = СтраныМира.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Страны мира'");
	КомандаВиджетаОбъект.Заголовок = "";
	КомандаВиджетаОбъект.Подсказка = "";
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "Справочник.СтраныМира";
	КомандаВиджетаОбъект.Картинка = "";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Структура предприятия
	КомандаВиджетаОбъект = СтруктураПредприятия.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Структура предприятия'");
	КомандаВиджетаОбъект.Заголовок = "";
	КомандаВиджетаОбъект.Подсказка = "";
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "Справочник.СтруктураПредприятия";
	КомандаВиджетаОбъект.Картинка = "";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Тематика документов
	КомандаВиджетаОбъект = ТематикиДокументов.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Тематика документов'");
	КомандаВиджетаОбъект.Заголовок = "";
	КомандаВиджетаОбъект.Подсказка = "";
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "Справочник.ТематикиДокументов";
	КомандаВиджетаОбъект.Картинка = "";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Тематики обращения
	КомандаВиджетаОбъект = ТематикиОбращений.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Тематики обращения'");
	КомандаВиджетаОбъект.Заголовок = "";
	КомандаВиджетаОбъект.Подсказка = "";
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "Справочник.ТематикиОбращений";
	КомандаВиджетаОбъект.Картинка = "";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Территории и помещения
	КомандаВиджетаОбъект = ТерриторииИПомещения.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Территории и помещения'");
	КомандаВиджетаОбъект.Заголовок = "";
	КомандаВиджетаОбъект.Подсказка = "";
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "Справочник.ТерриторииИПомещения";
	КомандаВиджетаОбъект.Картинка = "";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Типы связей
	КомандаВиджетаОбъект = ТипыСвязей.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Типы связей'");
	КомандаВиджетаОбъект.Заголовок = "";
	КомандаВиджетаОбъект.Подсказка = "";
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "Справочник.ТипыСвязей";
	КомандаВиджетаОбъект.Картинка = "";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Файлы
	КомандаВиджетаОбъект = Файлы.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Файлы'");
	КомандаВиджетаОбъект.Заголовок = "";
	КомандаВиджетаОбъект.Подсказка = "";
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "Справочник.Файлы.Команда.ПапкиФайлов";
	КомандаВиджетаОбъект.Картинка = "";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Физические лица
	КомандаВиджетаОбъект = ФизическиеЛица.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Физические лица'");
	КомандаВиджетаОбъект.Заголовок = "";
	КомандаВиджетаОбъект.Подсказка = "";
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "Справочник.ФизическиеЛица";
	КомандаВиджетаОбъект.Картинка = "";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
	// Шаблоны процессов
	КомандаВиджетаОбъект = ШаблоныПроцессов.ПолучитьОбъект();
	КомандаВиджетаОбъект.Наименование = НСтр("ru = 'Шаблоны процессов'");
	КомандаВиджетаОбъект.Заголовок = "";
	КомандаВиджетаОбъект.Подсказка = "";
	КомандаВиджетаОбъект.Условие = Справочники.АлгоритмыПроверки.ПустаяСсылка();
	КомандаВиджетаОбъект.Источник = Справочники.Реестры.ПустаяСсылка();
	КомандаВиджетаОбъект.ИсточникСтрока = "ОбщаяКоманда.ШаблоныБизнесПроцессов";
	КомандаВиджетаОбъект.Картинка = "";
	КомандаВиджетаОбъект.Действие = Перечисления.ДействияВиджетов.ОбработатьИсточник;
	КомандаВиджетаОбъект.ПараметрДействия1 = "";
	КомандаВиджетаОбъект.ПараметрДействия2 = "";
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(КомандаВиджетаОбъект);
	
КонецПроцедуры

// Создаёт команду "Открыть реестр".
//
// Параметры:
//  Реестр - СправочникСсылка.Реестры.
// 
// Возвращаемое значение:
//  СправочникСсылка.КомандыВиджетов - Команда "Открыть реестр".
//
Функция СоздатьКомандуОткрытьРеестр(Реестр) Экспорт
	
	УстановитьПривилегированныйРежим(Истина);
	
	Действие = Перечисления.ДействияВиджетов.ОткрытьРеестр;
	
	ДанныеЗаполнения = Новый Структура;
	ДанныеЗаполнения.Вставить("Наименование",
		СтрШаблон("%1 ""%2""",
			Действие,
			Реестр.УникальныйИдентификатор()));
	ДанныеЗаполнения.Вставить("Источник", Реестр);
	ДанныеЗаполнения.Вставить("Действие", Действие);
	
	КомандаОбъект = СоздатьЭлемент();
	КомандаОбъект.Заполнить(ДанныеЗаполнения);
	КомандаОбъект.Записать();
	
	Возврат КомандаОбъект.Ссылка;
	
КонецФункции

// Создаёт команду "Создать элемент реестра".
//
// Параметры:
//  Реестр - СправочникСсылка.Реестры.
// 
// Возвращаемое значение:
//  СправочникСсылка.КомандыВиджетов - Команда "Создать элемент реестра".
//
Функция СоздатьКомандуСоздатьЭлементРеестра(Реестр) Экспорт
	
	УстановитьПривилегированныйРежим(Истина);
	
	Действие = Перечисления.ДействияВиджетов.СоздатьЭлементРеестра;
	
	ДанныеЗаполнения = Новый Структура;
	ДанныеЗаполнения.Вставить("Наименование",
		СтрШаблон("%1 ""%2""",
			Действие,
			Реестр.УникальныйИдентификатор()));
	ДанныеЗаполнения.Вставить("Источник", Реестр);
	ДанныеЗаполнения.Вставить("Действие", Действие);
	ДанныеЗаполнения.Вставить("Картинка", "ВиджетБелыйСоздать");
	
	КомандаОбъект = СоздатьЭлемент();
	КомандаОбъект.Заполнить(ДанныеЗаполнения);
	КомандаОбъект.Записать();
	
	Возврат КомандаОбъект.Ссылка;
	
КонецФункции

// Данные команды виджета.
// 
// Возвращаемое значение:
//  Структура - Структура данные команды:
//   * КомандаВиджета - Строка.
//   * Заголовок - Строка.
//   * Подсказка - Строка.
//   * Картинка - Картинка, Неопределено.
//   * Источник - Строка, СправочникСсылка.Реестры.
//   * Действие - Структура, Неопределено.
//   * Условие - СправочникСсылка.АлгоритмыПроверки.
//   * ТипСтруктуры - Строка.
//
Функция НовыйДанныеКоманды() Экспорт
	
	ДанныеКоманды = Новый Структура;
	ДанныеКоманды.Вставить("КомандаВиджета", "");
	ДанныеКоманды.Вставить("Заголовок", "");
	ДанныеКоманды.Вставить("Подсказка", "");
	ДанныеКоманды.Вставить("Картинка");
	ДанныеКоманды.Вставить("Источник");
	ДанныеКоманды.Вставить("Действие");
	ДанныеКоманды.Вставить("Условие", Справочники.АлгоритмыПроверки.ПустаяСсылка());
	ДанныеКоманды.Вставить("ТипСтруктуры", "ДанныеКоманды");
	
	Возврат ДанныеКоманды;
	
КонецФункции

#КонецОбласти

#Область ОбработчикиСобытий

Процедура ОбработкаПолученияПолейПредставления(Поля, СтандартнаяОбработка)
	
	СтандартнаяОбработка = Ложь;
	Поля.Добавить("Наименование");
	Поля.Добавить("Действие");
	Поля.Добавить("Источник");
	
	МультиязычностьКлиентСервер.ОбработкаПолученияПолейПредставления(Поля, СтандартнаяОбработка);
	
КонецПроцедуры

Процедура ОбработкаПолученияПредставления(Данные, Представление, СтандартнаяОбработка)
	
	СтандартнаяОбработка = Ложь;
	
	МультиязычностьКлиентСервер.ОбработкаПолученияПредставления(Данные, Представление, СтандартнаяОбработка);
	Если ЗначениеЗаполнено(Представление) Тогда
		Данные.Наименование = Представление;
	КонецЕсли;
	
	Если Данные.Действие = Перечисления.ДействияВиджетов.СоздатьЭлементРеестра Тогда
		
		Представление = СтрШаблон("%1 ""%2""", Данные.Действие, Строка(Данные.Источник));
		
	ИначеЕсли Данные.Действие = Перечисления.ДействияВиджетов.ОткрытьРеестр Тогда
		
		Представление = Строка(Данные.Источник);
		
	Иначе
		
		Представление = Данные.Наименование;
		
	КонецЕсли;
	
КонецПроцедуры

Процедура ОбработкаПолученияДанныхВыбора(ДанныеВыбора, Параметры, СтандартнаяОбработка)
	
	СтандартнаяОбработка = Ложь;
	
	Запрос = Новый Запрос;
	
	Запрос.УстановитьПараметр("СтрокаПоиска", ?(Параметры.СтрокаПоиска = Неопределено, "", Параметры.СтрокаПоиска) + "%");
	
	Запрос.Текст =
	"ВЫБРАТЬ
	|	КомандыВиджетов.Ссылка КАК Ссылка
	|ИЗ
	|	Справочник.КомандыВиджетов КАК КомандыВиджетов
	|ГДЕ
	|	НЕ КомандыВиджетов.ПометкаУдаления
	|	И КомандыВиджетов.Наименование ПОДОБНО &СтрокаПоиска
	|
	|УПОРЯДОЧИТЬ ПО
	|	КомандыВиджетов.Наименование";
	
	ОбъектМетаданных = Метаданные.Справочники.КомандыВиджетов;
	Если Не МультиязычностьПовтИсп.КонфигурацияИспользуетТолькоОдинЯзык(ОбъектМетаданных.ТабличныеЧасти.Найти("Представления") = Неопределено) Тогда
		
		Если МультиязычностьСервер.ИспользуетсяДополнительныйЯзык(1) Тогда
			Запрос.Текст = СтрЗаменить(Запрос.Текст,
				"КомандыВиджетов.Наименование ПОДОБНО &СтрокаПоиска",
				"(КомандыВиджетов.Наименование ПОДОБНО &СтрокаПоиска ИЛИ КомандыВиджетов.НаименованиеЯзык1 ПОДОБНО &СтрокаПоиска)");
		КонецЕсли;
		
	КонецЕсли;
	
	ДанныеВыбора = Новый СписокЗначений;
	Выборка = Запрос.Выполнить().Выбрать();
	Пока Выборка.Следующий() Цикл
		ДанныеВыбора.Добавить(Выборка.Ссылка);
	КонецЦикла;
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

// Возвращает данные поставялемых команд.
Функция СформироватьДанныеКоманды(Выборка)
	
	ДанныеКоманды = НовыйДанныеКоманды();
	ДанныеКоманды.КомандаВиджета = Выборка.Ссылка;
	ДанныеКоманды.Заголовок = Выборка.Заголовок;
	ДанныеКоманды.Подсказка = Выборка.Подсказка;
	ДанныеКоманды.Условие = Выборка.Условие;
	ДанныеКоманды.Источник = ?(ЗначениеЗаполнено(Выборка.ИсточникСтрока),
		Выборка.ИсточникСтрока,
		Выборка.Источник);

	КонфигурацияИспользуетТолькоОдинЯзык = МультиязычностьПовтИсп.КонфигурацияИспользуетТолькоОдинЯзык(Ложь);
	Если Не КонфигурацияИспользуетТолькоОдинЯзык И Не МультиязычностьСервер.ЭтоОсновнойЯзык() Тогда
		ДанныеКоманды.Заголовок = Выборка.НаименованиеЯзык1; // обход
	КонецЕсли;

	Если ЗначениеЗаполнено(Выборка.Картинка) Тогда
		ДанныеКоманды.Картинка = БиблиотекаКартинок[Выборка.Картинка];
	КонецЕсли;
	
	Если ЗначениеЗаполнено(Выборка.Действие)
		И Выборка.Действие <> Перечисления.ДействияВиджетов.ОбработатьИсточник Тогда
		ДействиеВиджета = Выборка.Действие;
		ВыраженияПараметровДействия = Новый Массив;
		ВыраженияПараметровДействия.Добавить(Выборка.ПараметрДействия1);
		ВыраженияПараметровДействия.Добавить(Выборка.ПараметрДействия2);
		ДанныеКоманды.Действие = Перечисления.ДействияВиджетов.Действие(
			ДействиеВиджета,
			ДанныеКоманды.Источник,
			ВыраженияПараметровДействия);
	КонецЕсли;
	
	Если ТипЗнч(ДанныеКоманды.Источник) = Тип("СправочникСсылка.Реестры")
		И Не ЗначениеЗаполнено(Выборка.Заголовок) Тогда
		
		ДанныеКоманды.Заголовок = Выборка.ПредставлениеРеестра;
		Если ДанныеКоманды.Действие.ДействиеВиджета = Перечисления.ДействияВиджетов.СоздатьЭлементРеестра Тогда
			ДанныеКоманды.Заголовок = СтрШаблон(
				"%1 %2",
				НСтр("ru = 'Создать элемент реестра'"),
				Выборка.ПредставлениеРеестра);
		Иначе
			ДанныеКоманды.Заголовок = Выборка.ПредставлениеРеестра;
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат ДанныеКоманды;
	
КонецФункции

// Инициализирует данные команды.
Процедура ИнициализироватьКоманду(ДанныеКоманды)
	
	Если ЗначениеЗаполнено(ДанныеКоманды.Источник)
		И ТипЗнч(ДанныеКоманды.Источник) = Тип("Строка") Тогда
		
		МетаданныеИсточника = Метаданные.НайтиПоПолномуИмени(ДанныеКоманды.Источник);
		
		Если Не ЗначениеЗаполнено(ДанныеКоманды.Заголовок) Тогда
			
			Если ОбщегоНазначенияКлиентСервер.ЕстьРеквизитИлиСвойствоОбъекта(МетаданныеИсточника, "ПредставлениеСписка")
				И ЗначениеЗаполнено(МетаданныеИсточника.ПредставлениеСписка) Тогда
				ДанныеКоманды.Заголовок = МетаданныеИсточника.ПредставлениеСписка;
			Иначе
				ДанныеКоманды.Заголовок = МетаданныеИсточника.Синоним;
			КонецЕсли;
			
		КонецЕсли;
		
		Если Не ЗначениеЗаполнено(ДанныеКоманды.Подсказка)
			И ОбщегоНазначенияКлиентСервер.ЕстьРеквизитИлиСвойствоОбъекта(МетаданныеИсточника, "Подсказка") Тогда
			ДанныеКоманды.Подсказка = МетаданныеИсточника.Подсказка;
		КонецЕсли;
		
		Если ДанныеКоманды.Действие = Неопределено Тогда
			ДанныеКоманды.Действие = РаботаСВиджетами.ДействиеИсточника(ДанныеКоманды.Источник);
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры

// Проверяет доступность команды.
Функция КомандаДоступна(ДанныеКоманды)
	
	КомандаДоступна = РаботаСВиджетами.ИсточникДоступен(ДанныеКоманды.Источник)
		И РаботаСРабочимСтолом.ПроверитьУсловие(ДанныеКоманды.Условие);
	
	Возврат КомандаДоступна;
	
КонецФункции

#КонецОбласти

#КонецЕсли