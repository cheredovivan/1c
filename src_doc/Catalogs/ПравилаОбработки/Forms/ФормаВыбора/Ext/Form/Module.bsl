#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если Параметры.ТолькоВидыОбъектов Тогда 
		Список.Параметры.УстановитьЗначениеПараметра("ТолькоВидыОбъектов", Истина);
	КонецЕсли;
	
	Если Параметры.Свойство("ВидОбъекта") И ЗначениеЗаполнено(Параметры.ВидОбъекта) Тогда
		Список.Параметры.УстановитьЗначениеПараметра("ВидОбъекта", Параметры.ВидОбъекта);
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти