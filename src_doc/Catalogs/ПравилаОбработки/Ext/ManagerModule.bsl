#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ПрограммныйИнтерфейс

// Возвращает "Вид объекта" из правила верхнего уровня.
// 
// Параметры:
//  ПравилоОбработки - СправочникСсылка.ПравилаОбработки - Ссылка на правило обработки.
// 
// Возвращаемое значение:
//  ОпределяемыйТип.ВидОбъектаСОбработкой - ссылка на вид объекта.
//
Функция ВидОбъектаКорневогоПравила(ПравилоОбработки) Экспорт
 
	УстановитьПривилегированныйРежим(Истина);
	ВсеРодителиИЭлемент = Новый Массив();
	ВсеРодителиИЭлемент.Добавить(ПравилоОбработки);
	
	ОбщегоНазначенияКлиентСервер.ДополнитьМассив(ВсеРодителиИЭлемент,   
		ОбщегоНазначенияДокументооборот.ВсеРодителиЭлемента(ПравилоОбработки), Истина);
	КорневойРодитель = ВсеРодителиИЭлемент[ВсеРодителиИЭлемент.Количество() - 1];
	ВидОбъекта = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(
		КорневойРодитель, "ЭлементГруппировки");
	
	Возврат ВидОбъекта;
			
КонецФункции

// СтандартныеПодсистемы.ВерсионированиеОбъектов
// Определяет настройки объекта для подсистемы ВерсионированиеОбъектов.
//
// Параметры:
// Настройки - Структура - настройки подсистемы.
Процедура ПриОпределенииНастроекВерсионированияОбъектов(Настройки) Экспорт
	
КонецПроцедуры
// Конец СтандартныеПодсистемы.ВерсионированиеОбъектов

#КонецОбласти

#КонецЕсли
