#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ПрограммныйИнтерфейс

// Заполняет поставляемые данные.
// 
Процедура ЗаполнитьПоставляемыеДанные() Экспорт
	
	СостояниеЗадачОбъект = Черновик.ПолучитьОбъект();
	СостояниеЗадачОбъект.Наименование = НСтр("ru = 'Черновик'");
	СостояниеЗадачОбъект.ВидСостояния = Перечисления.ВидыСостоянийЗадач.Черновик;
	ОбновлениеИнформационнойБазыДокументооборот.ЗаписатьДанные(СостояниеЗадачОбъект);
	
	СостояниеЗадачОбъект = НаПроверке.ПолучитьОбъект();
	СостояниеЗадачОбъект.Наименование = НСтр("ru = 'На проверке'");
	СостояниеЗадачОбъект.ВидСостояния = Перечисления.ВидыСостоянийЗадач.НаОбработкеРезультата;
	ОбновлениеИнформационнойБазыДокументооборот.ЗаписатьДанные(СостояниеЗадачОбъект);
	
	СостояниеЗадачОбъект = Новая.ПолучитьОбъект();
	СостояниеЗадачОбъект.Наименование = НСтр("ru = 'Новая'");
	СостояниеЗадачОбъект.ВидСостояния = Перечисления.ВидыСостоянийЗадач.НоваяЗадача;
	ОбновлениеИнформационнойБазыДокументооборот.ЗаписатьДанные(СостояниеЗадачОбъект);
	
	СостояниеЗадачОбъект = ВРаботе.ПолучитьОбъект();
	СостояниеЗадачОбъект.Наименование = НСтр("ru = 'В работе'");
	СостояниеЗадачОбъект.ВидСостояния = Перечисления.ВидыСостоянийЗадач.ВРаботе;
	ОбновлениеИнформационнойБазыДокументооборот.ЗаписатьДанные(СостояниеЗадачОбъект);
	
	СостояниеЗадачОбъект = НаИсполнении.ПолучитьОбъект();
	СостояниеЗадачОбъект.Наименование = НСтр("ru = 'На исполнении'");
	СостояниеЗадачОбъект.ВидСостояния = Перечисления.ВидыСостоянийЗадач.ВРаботе;
	ОбновлениеИнформационнойБазыДокументооборот.ЗаписатьДанные(СостояниеЗадачОбъект);
	
	СостояниеЗадачОбъект = НаОзнакомлении.ПолучитьОбъект();
	СостояниеЗадачОбъект.Наименование = НСтр("ru = 'На ознакомлении'");
	СостояниеЗадачОбъект.ВидСостояния = Перечисления.ВидыСостоянийЗадач.ВРаботе;
	ОбновлениеИнформационнойБазыДокументооборот.ЗаписатьДанные(СостояниеЗадачОбъект);
	
	СостояниеЗадачОбъект = НаРассмотрении.ПолучитьОбъект();
	СостояниеЗадачОбъект.Наименование = НСтр("ru = 'На рассмотрении'");
	СостояниеЗадачОбъект.ВидСостояния = Перечисления.ВидыСостоянийЗадач.ВРаботе;
	ОбновлениеИнформационнойБазыДокументооборот.ЗаписатьДанные(СостояниеЗадачОбъект);
	
	СостояниеЗадачОбъект = НаРегистрации.ПолучитьОбъект();
	СостояниеЗадачОбъект.Наименование = НСтр("ru = 'На регистрации'");
	СостояниеЗадачОбъект.ВидСостояния = Перечисления.ВидыСостоянийЗадач.ВРаботе;
	ОбновлениеИнформационнойБазыДокументооборот.ЗаписатьДанные(СостояниеЗадачОбъект);
	
	СостояниеЗадачОбъект = НаСогласовании.ПолучитьОбъект();
	СостояниеЗадачОбъект.Наименование = НСтр("ru = 'На согласовании'");
	СостояниеЗадачОбъект.ВидСостояния = Перечисления.ВидыСостоянийЗадач.ВРаботе;
	ОбновлениеИнформационнойБазыДокументооборот.ЗаписатьДанные(СостояниеЗадачОбъект);
	
	СостояниеЗадачОбъект = НаУтверждении.ПолучитьОбъект();
	СостояниеЗадачОбъект.Наименование = НСтр("ru = 'На утверждении'");
	СостояниеЗадачОбъект.ВидСостояния = Перечисления.ВидыСостоянийЗадач.ВРаботе;
	ОбновлениеИнформационнойБазыДокументооборот.ЗаписатьДанные(СостояниеЗадачОбъект);
	
	СостояниеЗадачОбъект = НаОбработкеРезультата.ПолучитьОбъект();
	СостояниеЗадачОбъект.Наименование = НСтр("ru = 'На обработке результата'");
	СостояниеЗадачОбъект.ВидСостояния = Перечисления.ВидыСостоянийЗадач.НаОбработкеРезультата;
	ОбновлениеИнформационнойБазыДокументооборот.ЗаписатьДанные(СостояниеЗадачОбъект);
	
	СостояниеЗадачОбъект = НаОбработкеРезолюции.ПолучитьОбъект();
	СостояниеЗадачОбъект.Наименование = НСтр("ru = 'На обработке резолюции'");
	СостояниеЗадачОбъект.ВидСостояния = Перечисления.ВидыСостоянийЗадач.НаОбработкеРезультата;
	ОбновлениеИнформационнойБазыДокументооборот.ЗаписатьДанные(СостояниеЗадачОбъект);
	
	СостояниеЗадачОбъект = НаОзнакомленииСРезультатом.ПолучитьОбъект();
	СостояниеЗадачОбъект.Наименование = НСтр("ru = 'На ознакомлении с результатом'");
	СостояниеЗадачОбъект.ВидСостояния = Перечисления.ВидыСостоянийЗадач.НаОбработкеРезультата;
	ОбновлениеИнформационнойБазыДокументооборот.ЗаписатьДанные(СостояниеЗадачОбъект);
	
	СостояниеЗадачОбъект = Закрыта.ПолучитьОбъект();
	СостояниеЗадачОбъект.Наименование = НСтр("ru = 'Закрыта'");
	СостояниеЗадачОбъект.ВидСостояния = Перечисления.ВидыСостоянийЗадач.Закрыта;
	ОбновлениеИнформационнойБазыДокументооборот.ЗаписатьДанные(СостояниеЗадачОбъект);
	
	СостояниеЗадачОбъект = Исполнено.ПолучитьОбъект();
	СостояниеЗадачОбъект.Наименование = НСтр("ru = 'Исполнено'");
	СостояниеЗадачОбъект.ВидСостояния = Перечисления.ВидыСостоянийЗадач.Закрыта;
	ОбновлениеИнформационнойБазыДокументооборот.ЗаписатьДанные(СостояниеЗадачОбъект);
	
	СостояниеЗадачОбъект = Ознакомлены.ПолучитьОбъект();
	СостояниеЗадачОбъект.Наименование = НСтр("ru = 'Ознакомлены'");
	СостояниеЗадачОбъект.ВидСостояния = Перечисления.ВидыСостоянийЗадач.Закрыта;
	ОбновлениеИнформационнойБазыДокументооборот.ЗаписатьДанные(СостояниеЗадачОбъект);
	
	СостояниеЗадачОбъект = Принято.ПолучитьОбъект();
	СостояниеЗадачОбъект.Наименование = НСтр("ru = 'Принято'");
	СостояниеЗадачОбъект.ВидСостояния = Перечисления.ВидыСостоянийЗадач.Закрыта;
	ОбновлениеИнформационнойБазыДокументооборот.ЗаписатьДанные(СостояниеЗадачОбъект);
	
	СостояниеЗадачОбъект = Отклонено.ПолучитьОбъект();
	СостояниеЗадачОбъект.Наименование = НСтр("ru = 'Отклонено'");
	СостояниеЗадачОбъект.ВидСостояния = Перечисления.ВидыСостоянийЗадач.Закрыта;
	ОбновлениеИнформационнойБазыДокументооборот.ЗаписатьДанные(СостояниеЗадачОбъект);
	
	СостояниеЗадачОбъект = Рассмотрено.ПолучитьОбъект();
	СостояниеЗадачОбъект.Наименование = НСтр("ru = 'Рассмотрено'");
	СостояниеЗадачОбъект.ВидСостояния = Перечисления.ВидыСостоянийЗадач.Закрыта;
	ОбновлениеИнформационнойБазыДокументооборот.ЗаписатьДанные(СостояниеЗадачОбъект);
	
	СостояниеЗадачОбъект = Зарегистрировано.ПолучитьОбъект();
	СостояниеЗадачОбъект.Наименование = НСтр("ru = 'Зарегистрировано'");
	СостояниеЗадачОбъект.ВидСостояния = Перечисления.ВидыСостоянийЗадач.Закрыта;
	ОбновлениеИнформационнойБазыДокументооборот.ЗаписатьДанные(СостояниеЗадачОбъект);
	
	СостояниеЗадачОбъект = НеЗарегистрировано.ПолучитьОбъект();
	СостояниеЗадачОбъект.Наименование = НСтр("ru = 'Не зарегистрировано'");
	СостояниеЗадачОбъект.ВидСостояния = Перечисления.ВидыСостоянийЗадач.Закрыта;
	ОбновлениеИнформационнойБазыДокументооборот.ЗаписатьДанные(СостояниеЗадачОбъект);
	
	СостояниеЗадачОбъект = Согласовано.ПолучитьОбъект();
	СостояниеЗадачОбъект.Наименование = НСтр("ru = 'Согласовано'");
	СостояниеЗадачОбъект.ВидСостояния = Перечисления.ВидыСостоянийЗадач.Закрыта;
	ОбновлениеИнформационнойБазыДокументооборот.ЗаписатьДанные(СостояниеЗадачОбъект);
	
	СостояниеЗадачОбъект = СогласованоСЗамечаниями.ПолучитьОбъект();
	СостояниеЗадачОбъект.Наименование = НСтр("ru = 'Согласовано с замечаниями'");
	СостояниеЗадачОбъект.ВидСостояния = Перечисления.ВидыСостоянийЗадач.Закрыта;
	ОбновлениеИнформационнойБазыДокументооборот.ЗаписатьДанные(СостояниеЗадачОбъект);
	
	СостояниеЗадачОбъект = НеСогласовано.ПолучитьОбъект();
	СостояниеЗадачОбъект.Наименование = НСтр("ru = 'Не согласовано'");
	СостояниеЗадачОбъект.ВидСостояния = Перечисления.ВидыСостоянийЗадач.Закрыта;
	ОбновлениеИнформационнойБазыДокументооборот.ЗаписатьДанные(СостояниеЗадачОбъект);
	
	СостояниеЗадачОбъект = Утверждено.ПолучитьОбъект();
	СостояниеЗадачОбъект.Наименование = НСтр("ru = 'Утверждено'");
	СостояниеЗадачОбъект.ВидСостояния = Перечисления.ВидыСостоянийЗадач.Закрыта;
	ОбновлениеИнформационнойБазыДокументооборот.ЗаписатьДанные(СостояниеЗадачОбъект);
	
	СостояниеЗадачОбъект = НеУтверждено.ПолучитьОбъект();
	СостояниеЗадачОбъект.Наименование = НСтр("ru = 'Не утверждено'");
	СостояниеЗадачОбъект.ВидСостояния = Перечисления.ВидыСостоянийЗадач.Закрыта;
	ОбновлениеИнформационнойБазыДокументооборот.ЗаписатьДанные(СостояниеЗадачОбъект);
	
	СостояниеЗадачОбъект = Отложена.ПолучитьОбъект();
	СостояниеЗадачОбъект.Наименование = НСтр("ru = 'Отложена'");
	СостояниеЗадачОбъект.ВидСостояния = Перечисления.ВидыСостоянийЗадач.Отложена;
	ОбновлениеИнформационнойБазыДокументооборот.ЗаписатьДанные(СостояниеЗадачОбъект);
	
	СостояниеЗадачОбъект = Отменена.ПолучитьОбъект();
	СостояниеЗадачОбъект.Наименование = НСтр("ru = 'Отменена'");
	СостояниеЗадачОбъект.ВидСостояния = Перечисления.ВидыСостоянийЗадач.Отменена;
	ОбновлениеИнформационнойБазыДокументооборот.ЗаписатьДанные(СостояниеЗадачОбъект);
	
	СостояниеЗадачОбъект = Подписано.ПолучитьОбъект();
	СостояниеЗадачОбъект.Наименование = НСтр("ru = 'Подписано'");
	СостояниеЗадачОбъект.ВидСостояния = Перечисления.ВидыСостоянийЗадач.Закрыта;
	ОбновлениеИнформационнойБазыДокументооборот.ЗаписатьДанные(СостояниеЗадачОбъект);
	
	СостояниеЗадачОбъект = НеПодписано.ПолучитьОбъект();
	СостояниеЗадачОбъект.Наименование = НСтр("ru = 'Не подписано'");
	СостояниеЗадачОбъект.ВидСостояния = Перечисления.ВидыСостоянийЗадач.Закрыта;
	ОбновлениеИнформационнойБазыДокументооборот.ЗаписатьДанные(СостояниеЗадачОбъект);
	
	СостояниеЗадачОбъект = НаПодписании.ПолучитьОбъект();
	СостояниеЗадачОбъект.Наименование = НСтр("ru = 'На подписании'");
	СостояниеЗадачОбъект.ВидСостояния = Перечисления.ВидыСостоянийЗадач.ВРаботе;
	ОбновлениеИнформационнойБазыДокументооборот.ЗаписатьДанные(СостояниеЗадачОбъект);
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытий

Процедура ОбработкаПолученияПолейПредставления(Поля, СтандартнаяОбработка)
    МультиязычностьКлиентСервер.ОбработкаПолученияПолейПредставления(Поля, СтандартнаяОбработка);
КонецПроцедуры

Процедура ОбработкаПолученияПредставления(Данные, Представление, СтандартнаяОбработка)
    МультиязычностьКлиентСервер.ОбработкаПолученияПредставления(Данные, Представление, СтандартнаяОбработка);
КонецПроцедуры

Процедура ОбработкаПолученияДанныхВыбора(ДанныеВыбора, Параметры, СтандартнаяОбработка)
    МультиязычностьСервер.ОбработкаПолученияДанныхВыбора(ДанныеВыбора, 
    	Параметры, СтандартнаяОбработка, Метаданные.Справочники.СостоянияЗадач);
КонецПроцедуры

#КонецОбласти

#КонецЕсли
