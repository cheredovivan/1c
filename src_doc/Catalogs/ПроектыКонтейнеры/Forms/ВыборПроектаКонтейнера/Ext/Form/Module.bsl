#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Проект = Параметры.Проект;
	СпособВключенияПодчиненных = Параметры.СпособВключенияПодчиненных;
	
КонецПроцедуры

&НаКлиенте
Процедура Готово(Команда)

	ОчиститьСообщения();
	Если Не ПроверитьЗаполнение() Тогда
		Возврат;
	КонецЕсли;
	
	РезультатВыбора = ПараметрыЗакрытия();
	ОповеститьОВыборе(РезультатВыбора);
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

&НаСервере
Функция ПараметрыЗакрытия()
	
	Результат = Новый Структура;
	
	Результат.Вставить("Проект", Проект);
	Результат.Вставить("ПроектКонтейнер",
		Справочники.ПроектыКонтейнеры.НайтиСоздатьПроектКонтейнер(
			Проект, СпособВключенияПодчиненных));
	
	Возврат Результат;
	
КонецФункции

#КонецОбласти
