#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ПрограммныйИнтерфейс

// Заполняет поставляемые данные.
// 
Процедура ЗаполнитьПоставляемыеДанные() Экспорт
	
	РезультатЗадачОбъект = Зарегистрировано.ПолучитьОбъект();
	РезультатЗадачОбъект.Наименование = НСтр("ru = 'Зарегистрировано'");
	РезультатЗадачОбъект.ХарактерРезультата = Перечисления.ХарактерыРезультатов.Положительный;
	ОбновлениеИнформационнойБазыДокументооборот.ЗаписатьДанные(РезультатЗадачОбъект);
	
	РезультатЗадачОбъект = Исполнено.ПолучитьОбъект();
	РезультатЗадачОбъект.Наименование = НСтр("ru = 'Исполнено'");
	РезультатЗадачОбъект.ХарактерРезультата = Перечисления.ХарактерыРезультатов.Нейтральный;
	ОбновлениеИнформационнойБазыДокументооборот.ЗаписатьДанные(РезультатЗадачОбъект);
	
	РезультатЗадачОбъект = НеЗарегистрировано.ПолучитьОбъект();
	РезультатЗадачОбъект.Наименование = НСтр("ru = 'Не зарегистрировано'");
	РезультатЗадачОбъект.ХарактерРезультата = Перечисления.ХарактерыРезультатов.Отрицательный;
	ОбновлениеИнформационнойБазыДокументооборот.ЗаписатьДанные(РезультатЗадачОбъект);
	
	РезультатЗадачОбъект = НеПодписано.ПолучитьОбъект();
	РезультатЗадачОбъект.Наименование = НСтр("ru = 'Не подписано'");
	РезультатЗадачОбъект.ХарактерРезультата = Перечисления.ХарактерыРезультатов.Отрицательный;
	ОбновлениеИнформационнойБазыДокументооборот.ЗаписатьДанные(РезультатЗадачОбъект);
	
	РезультатЗадачОбъект = НеСогласовано.ПолучитьОбъект();
	РезультатЗадачОбъект.Наименование = НСтр("ru = 'Не согласовано'");
	РезультатЗадачОбъект.ХарактерРезультата = Перечисления.ХарактерыРезультатов.Отрицательный;
	ОбновлениеИнформационнойБазыДокументооборот.ЗаписатьДанные(РезультатЗадачОбъект);
	
	РезультатЗадачОбъект = НеУтверждено.ПолучитьОбъект();
	РезультатЗадачОбъект.Наименование = НСтр("ru = 'Не утверждено'");
	РезультатЗадачОбъект.ХарактерРезультата = Перечисления.ХарактерыРезультатов.Отрицательный;
	ОбновлениеИнформационнойБазыДокументооборот.ЗаписатьДанные(РезультатЗадачОбъект);
	
	РезультатЗадачОбъект = Ознакомлены.ПолучитьОбъект();
	РезультатЗадачОбъект.Наименование = НСтр("ru = 'Ознакомлены'");
	РезультатЗадачОбъект.ХарактерРезультата = Перечисления.ХарактерыРезультатов.Нейтральный;
	ОбновлениеИнформационнойБазыДокументооборот.ЗаписатьДанные(РезультатЗадачОбъект);
	
	РезультатЗадачОбъект = ОтмененоПриглашение.ПолучитьОбъект();
	РезультатЗадачОбъект.Наименование = НСтр("ru = 'Отменено приглашение'");
	РезультатЗадачОбъект.ХарактерРезультата = Перечисления.ХарактерыРезультатов.Отрицательный;
	ОбновлениеИнформационнойБазыДокументооборот.ЗаписатьДанные(РезультатЗадачОбъект);
	
	РезультатЗадачОбъект = Подписано.ПолучитьОбъект();
	РезультатЗадачОбъект.Наименование = НСтр("ru = 'Подписано'");
	РезультатЗадачОбъект.ХарактерРезультата = Перечисления.ХарактерыРезультатов.Положительный;
	ОбновлениеИнформационнойБазыДокументооборот.ЗаписатьДанные(РезультатЗадачОбъект);
	
	РезультатЗадачОбъект = ПодтвержденоПриглашение.ПолучитьОбъект();
	РезультатЗадачОбъект.Наименование = НСтр("ru = 'Подтверждено приглашение'");
	РезультатЗадачОбъект.ХарактерРезультата = Перечисления.ХарактерыРезультатов.Положительный;
	ОбновлениеИнформационнойБазыДокументооборот.ЗаписатьДанные(РезультатЗадачОбъект);
	
	РезультатЗадачОбъект = Рассмотрено.ПолучитьОбъект();
	РезультатЗадачОбъект.Наименование = НСтр("ru = 'Рассмотрено'");
	РезультатЗадачОбъект.ХарактерРезультата = Перечисления.ХарактерыРезультатов.Нейтральный;
	ОбновлениеИнформационнойБазыДокументооборот.ЗаписатьДанные(РезультатЗадачОбъект);
	
	РезультатЗадачОбъект = Согласовано.ПолучитьОбъект();
	РезультатЗадачОбъект.Наименование = НСтр("ru = 'Согласовано'");
	РезультатЗадачОбъект.ХарактерРезультата = Перечисления.ХарактерыРезультатов.Положительный;
	ОбновлениеИнформационнойБазыДокументооборот.ЗаписатьДанные(РезультатЗадачОбъект);
	
	РезультатЗадачОбъект = СогласованоСЗамечаниями.ПолучитьОбъект();
	РезультатЗадачОбъект.Наименование = НСтр("ru = 'Согласовано с замечаниями'");
	РезультатЗадачОбъект.ХарактерРезультата = Перечисления.ХарактерыРезультатов.ПоложительныйСЗамечаниями;
	ОбновлениеИнформационнойБазыДокументооборот.ЗаписатьДанные(РезультатЗадачОбъект);
	
	РезультатЗадачОбъект = Утверждено.ПолучитьОбъект();
	РезультатЗадачОбъект.Наименование = НСтр("ru = 'Утверждено'");
	РезультатЗадачОбъект.ХарактерРезультата = Перечисления.ХарактерыРезультатов.Положительный;
	ОбновлениеИнформационнойБазыДокументооборот.ЗаписатьДанные(РезультатЗадачОбъект);
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытий

Процедура ОбработкаПолученияПолейПредставления(Поля, СтандартнаяОбработка)
    МультиязычностьКлиентСервер.ОбработкаПолученияПолейПредставления(Поля, СтандартнаяОбработка);
КонецПроцедуры

Процедура ОбработкаПолученияПредставления(Данные, Представление, СтандартнаяОбработка)
    МультиязычностьКлиентСервер.ОбработкаПолученияПредставления(Данные, Представление, СтандартнаяОбработка);
КонецПроцедуры

Процедура ОбработкаПолученияДанныхВыбора(ДанныеВыбора, Параметры, СтандартнаяОбработка)
    МультиязычностьСервер.ОбработкаПолученияДанныхВыбора(ДанныеВыбора, 
    Параметры, СтандартнаяОбработка, Метаданные.Справочники.РезультатыЗадач);
КонецПроцедуры

#КонецОбласти

#КонецЕсли
