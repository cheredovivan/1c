
#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	ВидыСкриптов = ВидыСкриптов();
	ВидСкрипта = ВидыСкриптов[0].Значение;
	Элементы.СтраницыСкриптов.ТекущаяСтраница = Элементы["Страница" + ВидСкрипта];
	
	Элементы.ВидСкрипта.СписокВыбора.Очистить();
	Для Каждого Эл Из ВидыСкриптов Цикл
		Элементы.ВидСкрипта.СписокВыбора.Добавить(Эл.Значение, Эл.Представление);
	КонецЦикла;
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовШапкиФормы

&НаКлиенте
Процедура ВидСкриптаПриИзменении(Элемент)
	
	Элементы.СтраницыСкриптов.ТекущаяСтраница = Элементы["Страница" + ВидСкрипта];
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовТаблицыФормыСписокПравилаАвтозаполненияФайлов

&НаКлиенте
Процедура СписокПравилаАвтозаполненияФайловВыбор(Элемент, ВыбраннаяСтрока, Поле, СтандартнаяОбработка)
	
	ТекущиеДанные = Элемент.ТекущиеДанные;
	Если ТекущиеДанные <> Неопределено Тогда
		ПоказатьЗначение(, ТекущиеДанные.Ссылка);
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

// Краткое словеное описание назначения и/или принципов работы функции.
//
// Параметры:
//  ИмяПараметра - Список типов - Текстовое описание параметра функции.
//   * ИмяСвойстваПараметра - Список типов - Текстовое описание свойства (колонки) параметра.
//
// Возвращаемое значение:
//  Тип - Текстовое описание содержания возвращаемого значения функции.
//
&НаСервере
Функция ВидыСкриптов()
	
	ВидыСкриптов = Новый СписокЗначений;
	ВидыСкриптов.Добавить("АвтоподстановкиДляОбъектов", НСтр("ru = 'Автоподстановки для объектов'"));
	ВидыСкриптов.Добавить("АвтоподстановкиДляПроцессов", НСтр("ru = 'Автоподстановки для процессов'"));
	ВидыСкриптов.Добавить("Алгоритмы", НСтр("ru = 'Алгоритмы'"));
	
	Если ПолучитьФункциональнуюОпцию("ИспользоватьМетрики") Тогда
		ВидыСкриптов.Добавить("Метрики", НСтр("ru = 'Метрики'"));
	КонецЕсли;	
	
	ВидыСкриптов.Добавить("ПользовательскиеОбработчикиБизнесСобытий", НСтр("ru = 'Пользовательские обработчики бизнес-событий'"));
	ВидыСкриптов.Добавить("ПравилаАвтозаполненияФайлов", НСтр("ru = 'Настройки заполнения файлов'"));
	ВидыСкриптов.Добавить("СкриптыСхемДляОбработокОбъектов", НСтр("ru = 'Скрипты схем обработок объектов'"));
	ВидыСкриптов.Добавить("УсловияЗадач", НСтр("ru = 'Условия задач'"));
	ВидыСкриптов.Добавить("АлгоритмыПроверки", НСтр("ru = 'Алгоритмы проверки'"));
	Если ПолучитьФункциональнуюОпцию("ИспользоватьЧатБота") Тогда
		ВидыСкриптов.Добавить("ВычисляемыеПараметрыЧатБота", НСтр("ru = 'Вычисляемые параметры чат-бота'"));
	КонецЕсли;
	
	ВидыСкриптов.Добавить("СкриптыСхемДляКомплексныхПроцессов", НСтр("ru = 'Скрипты схем комплексных процессов'"));
	ВидыСкриптов.Добавить("СкриптыУсловийСхемДляКомплексныхПроцессов", НСтр("ru = 'Скрипты для условий схем комплексных процессов'"));
		
	ВидыСкриптов.СортироватьПоПредставлению();
	
	Возврат ВидыСкриптов;
	
КонецФункции

#КонецОбласти
