#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
		
	ДопустимыеТипы.ЗагрузитьЗначения(Параметры.ДопустимыеТипы);
	
	ТекущийТипЭлемента = Параметры.ТекущийТипЭлемента; 
	
	Элементы.ДопустимыеТипы.ТекущаяСтрока =
		ДопустимыеТипы.НайтиПоЗначению(ТекущийТипЭлемента).ПолучитьИдентификатор();
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовТаблицыФормы_ДопустимыеТипы

&НаКлиенте
Процедура ДопустимыеТипыПриАктивизацииСтроки(Элемент)
	
	ТекущийТипЭлемента = Неопределено;	
	ТекущиеДанные = Элементы.ДопустимыеТипы.ТекущиеДанные;
	Если ТекущиеДанные <> Неопределено Тогда
		ТекущийТипЭлемента = ТекущиеДанные.Значение;
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ДопустимыеТипыВыбор(Элемент, ВыбраннаяСтрока, Поле, СтандартнаяОбработка)
	
	Закрыть(ТекущийТипЭлемента);
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура ОК(Команда)
	
	Закрыть(ТекущийТипЭлемента);
	
КонецПроцедуры

#КонецОбласти