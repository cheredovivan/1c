
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	ПрежнийТекст = Параметры.ПрежнийТекст;
	НовыйТекст = ПрежнийТекст;
	
	Если Параметры.Свойство("ФайлHTML") И Не ЗначениеЗаполнено(Параметры.ФайлHTML) Тогда
		Элементы.ПрежнийТекст.ТолькоПросмотр = Ложь;
	КонецЕсли;	
	
КонецПроцедуры

&НаКлиенте
Процедура ОК(Команда)
	
	Закрыть(
		Новый Структура("ПрежнийТекст, НовыйТекст", ПрежнийТекст, НовыйТекст)
	);
	
КонецПроцедуры
