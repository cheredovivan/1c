
#Область ОбработчикиСобытийФормы

&НаКлиенте
Процедура ПриОткрытии(Отказ)
	
	ПодключитьОбработчикОжидания("ОбновитьСостояниеОжидания", 3, Ложь);
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура ВыйтиИзРежимаСканирования(Команда)
	
	Закрыть();
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

&НаКлиенте
Процедура ОбновитьСостояниеОжидания()
	
	Владелец = ВладелецФормы;
	Если МК_ВызовСервера.СканыЗагружены() Тогда
		КоличествоСкановТекущее = 0;
		СканДобавлен = МК_Клиент.ОбработатьДанныеСканов(Владелец.Объект.Ссылка, КоличествоСкановТекущее);
		Если СканДобавлен Тогда
			КоличествоДобавленныхСканов = КоличествоДобавленныхСканов + КоличествоСкановТекущее;
		КонецЕсли;
	КонецЕсли;

КонецПроцедуры

#КонецОбласти