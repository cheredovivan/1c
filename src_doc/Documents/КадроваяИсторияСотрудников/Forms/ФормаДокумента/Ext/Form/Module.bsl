#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПослеЗаписиНаСервере(ТекущийОбъект, ПараметрыЗаписи)
	
	СписокФизЛиц = Новый Массив;
	Для Каждого Строка Из Объект.Записи Цикл
		Если СписокФизЛиц.Найти(Строка.ФизическоеЛицо) = Неопределено Тогда
			СписокФизЛиц.Добавить(Строка.ФизическоеЛицо);
		КонецЕсли;
	КонецЦикла;
	
	РегистрыСведений.СоответствиеСотрудниковИССотрудникамДО.ЗаполнитьСоответствия(СписокФизЛиц);
	
КонецПроцедуры

#КонецОбласти