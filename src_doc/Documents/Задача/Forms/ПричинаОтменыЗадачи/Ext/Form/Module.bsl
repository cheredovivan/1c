#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	КлючСохраненияПоложенияОкна = "";
	
	Задача = Параметры.Задача;
	
	Элементы.КтоОтменил.Видимость = ЗначениеЗаполнено(Задача.КтоОтменил);
	Элементы.ДатаОтмены.Видимость = ЗначениеЗаполнено(Задача.ДатаОтмены);
	
	КлючСохраненияПоложенияОкна =
		Строка(Элементы.КтоОтменил.Видимость)
		+ Строка(Элементы.ДатаОтмены.Видимость);
	
КонецПроцедуры

#КонецОбласти