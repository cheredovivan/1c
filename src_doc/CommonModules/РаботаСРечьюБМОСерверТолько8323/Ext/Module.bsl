///////////////////////////////////////////////////////////////////////////////////////////////////////
// Copyright (c) 2022, ООО 1С-Софт
// Все права защищены. Эта программа и сопроводительные материалы предоставляются 
// в соответствии с условиями лицензии Attribution 4.0 International (CC BY 4.0)
// Текст лицензии доступен по ссылке:
// https://creativecommons.org/licenses/by/4.0/legalcode
///////////////////////////////////////////////////////////////////////////////////////////////////////

//
// Модуль корректно компилируется и работает только на платформе 8.3.23
// На платформе младшей версии возможны ошибки статанализа и исполнения
//

#Область СлужебныйПрограммныйИнтерфейс

Функция ПоддерживаетсяИспользованиеВнешнегоРасположения() Экспорт
	
#Если ТолстыйКлиентУправляемоеПриложение Тогда
	Возврат Ложь;
#Иначе
	Возврат РаботаСРечью.ПоддерживаетсяИспользованиеВнешнегоРасположения();
#КонецЕсли
	
КонецФункции

Функция НовыйПараметрыВнешнегоПодключенияРаботыСРечью(Токен, Адрес) Экспорт
	
#Если ТолстыйКлиентУправляемоеПриложение Тогда
	ВызватьИсключение НСтр("ru = 'Распознавание речи не поддерживается в толстом клиенте.'");
#Иначе
	Если Адрес = Неопределено Тогда
		Возврат Новый ПараметрыВнешнегоПодключенияРаботыСРечью(Токен);
	Иначе
		Возврат Новый ПараметрыВнешнегоПодключенияРаботыСРечью(Токен, Адрес);
	КонецЕсли;
#КонецЕсли
	
КонецФункции

Функция ВариантИспользованияРасположенияРаботыСРечью() Экспорт
	
#Если ТолстыйКлиентУправляемоеПриложение Тогда
	ВызватьИсключение НСтр("ru = 'Распознавание речи не поддерживается в толстом клиенте.'");
#Иначе
	Возврат ВариантИспользованияРасположенияРаботыСРечью;
#КонецЕсли
	
КонецФункции

Процедура ВыполнитьРегистрациюСеанса(Логин, Пароль, Адрес) Экспорт
	
#Если ТолстыйКлиентУправляемоеПриложение Тогда
	ВызватьИсключение НСтр("ru = 'Распознавание речи не поддерживается в толстом клиенте.'");
#Иначе
	Если Адрес = Неопределено Тогда 
		РаботаСРечью.ВыполнитьРегистрациюСеанса(Логин, Пароль);
	Иначе
		РаботаСРечью.ВыполнитьРегистрациюСеанса(Логин, Пароль, Адрес);
	КонецЕсли;
#КонецЕсли
	
КонецПроцедуры

Функция ПолучитьПараметрыВнешнегоПодключенияИнформационнойБазы() Экспорт
	
#Если ТолстыйКлиентУправляемоеПриложение Тогда
	ВызватьИсключение НСтр("ru = 'Распознавание речи не поддерживается в толстом клиенте.'");
#Иначе
	Возврат РаботаСРечью.ПолучитьПараметрыВнешнегоПодключенияИнформационнойБазы();
#КонецЕсли
	
КонецФункции

Процедура УстановитьПараметрыВнешнегоПодключенияИнформационнойБазы(Параметры) Экспорт
	
#Если ТолстыйКлиентУправляемоеПриложение Тогда
	ВызватьИсключение НСтр("ru = 'Распознавание речи не поддерживается в толстом клиенте.'");
#Иначе
	РаботаСРечью.УстановитьПараметрыВнешнегоПодключенияИнформационнойБазы(Параметры);
#КонецЕсли
	
КонецПроцедуры

Функция ПолучитьПараметрыВнешнегоПодключенияСеанса() Экспорт
	
#Если ТолстыйКлиентУправляемоеПриложение Тогда
	ВызватьИсключение НСтр("ru = 'Распознавание речи не поддерживается в толстом клиенте.'");
#Иначе
	Возврат РаботаСРечью.ПолучитьПараметрыВнешнегоПодключенияСеанса();
#КонецЕсли
	
КонецФункции

Процедура УстановитьПараметрыВнешнегоПодключенияСеанса(Параметры) Экспорт
	
#Если ТолстыйКлиентУправляемоеПриложение Тогда
	ВызватьИсключение НСтр("ru = 'Распознавание речи не поддерживается в толстом клиенте.'");
#Иначе
	РаботаСРечью.УстановитьПараметрыВнешнегоПодключенияСеанса(Параметры);
#КонецЕсли
	
КонецПроцедуры

Функция НовыйИдентификаторМоделиРаспознаванияРечи(ИдентификаторМодели) Экспорт
	
#Если ТолстыйКлиентУправляемоеПриложение Тогда
	ВызватьИсключение НСтр("ru = 'Распознавание речи не поддерживается в толстом клиенте.'");
#Иначе
	Возврат Новый ИдентификаторМоделиРаспознаванияРечи(ИдентификаторМодели);
#КонецЕсли
	
КонецФункции

Функция ПолучитьОписаниеМоделейРаспознавания() Экспорт
	
#Если ТолстыйКлиентУправляемоеПриложение Тогда
	ВызватьИсключение НСтр("ru = 'Распознавание речи не поддерживается в толстом клиенте.'");
#Иначе
	Возврат РаботаСРечью.ПолучитьОписаниеМоделейРаспознавания();
#КонецЕсли
	
КонецФункции

Функция ПолучитьМодельРаспознаванияВоВременноеХранилище(ИдентификаторМодели) Экспорт
	
#Если ТолстыйКлиентУправляемоеПриложение Тогда
	ВызватьИсключение НСтр("ru = 'Распознавание речи не поддерживается в толстом клиенте.'");
#Иначе
	Возврат РаботаСРечью.ПолучитьМодельРаспознаванияВоВременноеХранилище(ИдентификаторМодели);
#КонецЕсли
	
КонецФункции

Процедура УстановитьМодельРаспознаванияДляИнформационнойБазы(Источник) Экспорт 
	
#Если ТолстыйКлиентУправляемоеПриложение Тогда
	ВызватьИсключение НСтр("ru = 'Распознавание речи не поддерживается в толстом клиенте.'");
#Иначе
	РаботаСРечью.УстановитьМодельРаспознаванияДляИнформационнойБазы(Источник);
#КонецЕсли
	
КонецПроцедуры

Процедура УдалитьМодельРаспознаванияДляИнформационнойБазы(ИдентификаторМодели) Экспорт
	
#Если ТолстыйКлиентУправляемоеПриложение Тогда
	ВызватьИсключение НСтр("ru = 'Распознавание речи не поддерживается в толстом клиенте.'");
#Иначе
	РаботаСРечью.УдалитьМодельРаспознаванияДляИнформационнойБазы(ИдентификаторМодели);
#КонецЕсли
	
КонецПроцедуры

Процедура УстановитьДополнительнуюГрамматикуСеанса(Ключ, ДополнительнаяГрамматика) Экспорт
	
#Если ТолстыйКлиентУправляемоеПриложение Тогда
	ВызватьИсключение НСтр("ru = 'Распознавание речи не поддерживается в толстом клиенте.'");
#Иначе
	РаботаСРечью.УстановитьДополнительнуюГрамматикуСеанса(Ключ, ДополнительнаяГрамматика);
#КонецЕсли
	
КонецПроцедуры

Процедура УдалитьДополнительнуюГрамматикуСеанса(Ключ) Экспорт
	
#Если ТолстыйКлиентУправляемоеПриложение Тогда
	ВызватьИсключение НСтр("ru = 'Распознавание речи не поддерживается в толстом клиенте.'");
#Иначе
	РаботаСРечью.УдалитьДополнительнуюГрамматикуСеанса(Ключ);
#КонецЕсли
	
КонецПроцедуры

#КонецОбласти