////////////////////////////////////////////////////////////////////////////////
// Шаблоны текстов (вызов сервера).
//
////////////////////////////////////////////////////////////////////////////////

#Область ПрограммныйИнтерфейс

// Возвращает данные шаблона.
//
// Параметры:
//  ШаблонТекста - СправочникСсылка.ШаблоныТекстов.
// 
// Возвращаемое значение:
//  Структура - Данные шаблона.
//   * ТекстШаблона - Строка.
//   * РезультатДействия - СправочникСсылка.РезультатыДействийЗадач.
//
Функция ДанныеШаблона(Знач ШаблонТекста) Экспорт
	
	ДанныеШаблона = Новый Структура;
	ДанныеШаблона.Вставить("ТекстШаблона",
		Справочники.ШаблоныТекстов.ПолучитьТекстШаблона(ШаблонТекста));
	ДанныеШаблона.Вставить("РезультатДействия",
		ОбщегоНазначения.ЗначениеРеквизитаОбъекта(ШаблонТекста, "РезультатДействия"));
	
	Возврат ДанныеШаблона;
	
КонецФункции

// Возвращает текст шаблона.
//
// Параметры:
//  ШаблонТекста - СправочникСсылка.ШаблоныТекстов.
// 
// Возвращаемое значение:
//  Строка - Текст шаблона.
//
Функция ТекстШаблона(Знач ШаблонТекста) Экспорт
	
	Возврат Справочники.ШаблоныТекстов.ПолучитьТекстШаблона(ШаблонТекста);
	
КонецФункции

#КонецОбласти