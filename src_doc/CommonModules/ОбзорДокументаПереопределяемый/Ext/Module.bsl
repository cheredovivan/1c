
// Переопределяемая функция получения обзора документа предприятия в виде HTML текста.
//
// Параметры:
//    Форма - ФормаКлиентскогоПриложения - форма, из которой формируется обзор документа
//
// Возвращаемое значение:
//    HTMLТекст - Строка - обзор переданного объекта в виде HTML текста
//
Функция ПолучитьОбзорДокумента(Форма) Экспорт 
	
	Объект = Форма.Объект;
	КэшДокументыПредприятияФормаЭлемента =
		КэшиНаВремяВызоваПовтИсп.КэшДокументыПредприятияФормаЭлемента();
	КэшОбзораДокумента = КэшиНаВремяВызова.КэшОбзораДокумента();
	Если КэшДокументыПредприятияФормаЭлемента.Инициализирован
		И КэшДокументыПредприятияФормаЭлемента.КэшДокумента.ВидДокумента = Объект.ВидДокумента Тогда
		ВестиУчетПоКонтрагентам = ЗначениеЗаполнено(КэшДокументыПредприятияФормаЭлемента.КэшДокумента.ВидДокумента)
			И КэшДокументыПредприятияФормаЭлемента.КэшВидаДокумента.ВестиУчетПоКонтрагентам;
	ИначеЕсли КэшОбзораДокумента.Заполнен И КэшОбзораДокумента.ВидДокумента = Объект.ВидДокумента Тогда
		ВестиУчетПоКонтрагентам = КэшОбзораДокумента.КэшВидаДокумента.ВестиУчетПоКонтрагентам;
	Иначе
		ВестиУчетПоКонтрагентам = ЗначениеЗаполнено(Объект.ВидДокумента) 
			И ОбщегоНазначения.ЗначениеРеквизитаОбъекта(Объект.ВидДокумента, "ВестиУчетПоКонтрагентам");
	КонецЕсли;
	Если ВестиУчетПоКонтрагентам Тогда
		Возврат ПолучитьОбзорДокументаСКонтрагентом(Форма)
	Иначе
		Возврат ПолучитьОбзорДокументаБезКонтрагента(Форма);
	КонецЕсли;
	
КонецФункции	

Функция ПолучитьОбзорДокументаСКонтрагентом(Форма) 
	
	Возврат "";
	
КонецФункции

Функция ПолучитьОбзорДокументаБезКонтрагента(Форма) 
	
	Возврат "";
	
КонецФункции

// Переопределяемая функция получения обзора шаблона документа предприятия
Функция ПолучитьОбзорШаблонаДокумента(ШаблонСсылка) Экспорт 
	
	ВидДокумента = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(ШаблонСсылка, "ВидДокумента");
	
	Если ЗначениеЗаполнено(ВидДокумента) 
		И ОбщегоНазначения.ЗначениеРеквизитаОбъекта(ВидДокумента, "ВестиУчетПоКонтрагентам") Тогда
		Возврат ПолучитьОбзорШаблонаДокументаСКонтрагентом(ШаблонСсылка)
	Иначе	
		Возврат ПолучитьОбзорШаблонаДокументаБезКонтрагента(ШаблонСсылка);
	КонецЕсли;
	
КонецФункции	

Функция ПолучитьОбзорШаблонаДокументаСКонтрагентом(ШаблонСсылка) 
	
	Возврат "";
	
КонецФункции

Функция ПолучитьОбзорШаблонаДокументаБезКонтрагента(ШаблонСсылка) 
	
	Возврат "";
	
КонецФункции

// Переопределяемая функция получения обзора исходящего документа в виде HTML текста.
//
// Параметры:
//    Форма - ФормаКлиентскогоПриложения - форма, из которой формируется обзор документа
//
// Возвращаемое значение:
//    HTMLТекст - Строка - обзор переданного объекта в виде HTML текста
//
Функция ПолучитьОбзорИсходящегоДокумента(Форма) Экспорт 
	
	HTMLТекст = "";
	// Укажите свое представление документа.
	
	Возврат HTMLТекст;
	
КонецФункции

// Переопределяемая функция получения обзора входящего документа в виде HTML текста.
//
// Параметры:
//    Форма - ФормаКлиентскогоПриложения - форма, из которой формируется обзор документа
//
// Возвращаемое значение:
//    HTMLТекст - Строка - обзор переданного объекта в виде HTML текста
//
Функция ПолучитьОбзорВходящегоДокумента(Форма) Экспорт 
	
	HTMLТекст = "";
	// Укажите свое представление документа.
	
	Возврат HTMLТекст;
	
КонецФункции
