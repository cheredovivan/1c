////////////////////////////////////////////////////////////////////////////////
// Помощник отправить (клиент/сервер): Содержит интерфейсные функции для работы
//                                     с помощником отправки.
//  
////////////////////////////////////////////////////////////////////////////////

#Область ПрограммныйИнтерфейс

// Проверяет, что переданный вариант является вариантом СоздатьПисьмоНаОсновании.
//
// Параметры:
//  Вариант - Произвольный - вариант помощника.
//
// Возвращаемое значение:
//  Булево
//
Функция ЭтоВариантСоздатьПисьмоНаОсновании(Вариант) Экспорт
	
	Если Вариант = ВариантОтправкиСоздатьПисьмоНаОсновании() Тогда
		Возврат Истина;
	КонецЕсли;
	
	Возврат Ложь;
	
КонецФункции

// Проверяет, что переданный вариант является вариантом создания процесса.
//
// Параметры:
//  Вариант - Произвольный - вариант помощника.
//
// Возвращаемое значение:
//  Булево
//
Функция ЭтоВариантСозданияПроцесса(Вариант) Экспорт
	
	Возврат ШаблоныБизнесПроцессовКлиентСервер.ЭтоШаблонПроцесса(Вариант)
		Или Вариант = ГруппаДругихВариантовСозданияПроцессов();
	
КонецФункции

// Проверяет, что переданный вариант является вариантом создания задачи.
//
// Параметры:
//  Вариант - Произвольный - вариант помощника.
//
// Возвращаемое значение:
//  Булево
//
Функция ЭтоВариантСозданияЗадачи(Вариант) Экспорт
	
	Возврат ЭтоВариантСозданияПоручение(Вариант)
		Или ЭтоВариантСозданияОзнакомления(Вариант)
		Или ЭтоВариантСозданияПоШаблону(Вариант);
	
КонецФункции

// Проверяет, что переданный вариант является вариантом создания поручение.
//
// Параметры:
//  Вариант - Произвольный - вариант помощника.
//
// Возвращаемое значение:
//  Булево
//
Функция ЭтоВариантСозданияПоручение(Вариант) Экспорт
	
	Возврат Вариант = ВариантОтправкиСоздатьПоручение();
	
КонецФункции

// Проверяет, что переданный вариант является вариантом создания ознакомления.
//
// Параметры:
//  Вариант - Произвольный - вариант помощника.
//
// Возвращаемое значение:
//  Булево
//
Функция ЭтоВариантСозданияОзнакомления(Вариант) Экспорт
	
	Возврат Вариант = ВариантОтправкиСоздатьОзнакомление();
	
КонецФункции

// Проверяет, что переданный вариант является вариантом создания задачи по шаблону.
//
// Параметры:
//  Вариант - Произвольный - вариант помощника.
//
// Возвращаемое значение:
//  Булево
//
Функция ЭтоВариантСозданияПоШаблону(Вариант) Экспорт
	
	Возврат ТипЗнч(Вариант) = Тип("СправочникСсылка.ШаблоныЗадач");
	
КонецФункции

// Проверяет, что переданный вариант является вариантом создания обработки.
//
// Параметры:
//  Вариант - Произвольный - вариант помощника.
//
// Возвращаемое значение:
//  Булево
//
Функция ЭтоВариантСозданияОбработки(Вариант) Экспорт
	
	Возврат Вариант = ВариантОтправкиСоздатьОбработку();
	
КонецФункции

#КонецОбласти

#Область ПрограммныйИнтерфейс_РежимыРаботы

// Возвращает режим работы ОтправкаОбъектов.
//
// Возвращаемое значение:
//  Строка
//
Функция РежимРаботыОтправкаОбъектов() Экспорт
	
	Возврат "ОтправкаОбъектов";
	
КонецФункции

// Возвращает режим работы ОтправкаПроцесса.
//
// Возвращаемое значение:
//  Строка
//
Функция РежимРаботыОтправкаПроцесса() Экспорт
	
	Возврат "ОтправкаПроцесса";
	
КонецФункции

// Возвращает режим работы ОтправкаПодзадачи.
//
// Возвращаемое значение:
//  Строка
//
Функция РежимРаботыОтправкаПодзадачи() Экспорт
	
	Возврат "ОтправкаПодзадачи";
	
КонецФункции

// Возвращает режим работы ОтправкаЗадачи.
//
// Возвращаемое значение:
//  Строка
//
Функция РежимРаботыОтправкаЗадачи() Экспорт
	
	Возврат "ОтправкаЗадачи";
	
КонецФункции

// Возвращает режим работы ОтправкаПользователям.
//
// Возвращаемое значение:
//  Строка
//
Функция РежимРаботыОтправкаПользователям() Экспорт
	
	Возврат "ОтправкаПользователям";
	
КонецФункции

// Возвращает режим работы ОтправкаСотрудникам.
//
// Возвращаемое значение:
//  Строка
//
Функция РежимРаботыОтправкаСотрудникам() Экспорт
	
	Возврат "ОтправкаСотрудникам";
	
КонецФункции

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

// Возвращает вид варианта отправки СоздатьПисьмоНаОсновании.
//
// Возвращаемое значение:
//  Строка
//
Функция ВариантОтправкиСоздатьПисьмоНаОсновании() Экспорт
	
	Возврат "СоздатьПисьмоНаОсновании";
	
КонецФункции

// Возвращает вид варианта отправки СоздатьОбработку.
//
// Возвращаемое значение:
//  Строка
//
Функция ВариантОтправкиСоздатьОбработку() Экспорт
	
	Возврат "СоздатьОбработку";
	
КонецФункции

// Возвращает вид варианта отправки СоздатьПоручение.
//
// Возвращаемое значение:
//  Строка
//
Функция ВариантОтправкиСоздатьПоручение() Экспорт
	
	Возврат "СоздатьПоручение";
	
КонецФункции

// Возвращает вид варианта отправки СоздатьОзнакомление.
//
// Возвращаемое значение:
//  Строка
//
Функция ВариантОтправкиСоздатьОзнакомление() Экспорт
	
	Возврат "СоздатьОзнакомление";
	
КонецФункции

// Возвращает вид варианта отправки Еще.
//
// Возвращаемое значение:
//  Строка
//
Функция ГруппаДругихВариантовСозданияПроцессов() Экспорт
	
	Возврат "ДругиеВариантыСозданияПроцессов";
	
КонецФункции

#КонецОбласти