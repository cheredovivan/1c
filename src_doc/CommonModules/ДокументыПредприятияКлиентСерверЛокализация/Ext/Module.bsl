
#Область ПрограммныйИнтерфейс

#Область ФормаЭлемента

// Проверяет, является ли переданная строка спика файлов в форме документа строкой с файлом
// 
// Параметры:
//  Строка - СтрокаТаблицыЗначений
// 
// Возвращаемое значение:
//  Булево - Строка файлов не является файлом
Функция СтрокаФайловНеЯвляетсяФайлом(Строка) Экспорт
// Локализация
	Если Строка.ЭтоДокументЭДО Тогда
		Возврат Истина;
	КонецЕсли;
// Локализация Конец
	Возврат Ложь;
КонецФункции

#КонецОбласти

#КонецОбласти
