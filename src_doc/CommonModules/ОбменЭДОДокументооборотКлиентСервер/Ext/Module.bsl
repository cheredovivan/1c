// @strict-types

#Область ПрограммныйИнтерфейс

#Область АвтоматическоеСозданиеОбъектовПоДаннымЭД

// Возвращает описание подсистемы ЭДО.
//
// Возвращаемое значение:
//	Структура:
//		* Имя - Строка.
//		* Представление - Строка.
//
Функция ОписаниеПодсистемыЭДО() Экспорт
	
	Описание = Новый Структура;
	Описание.Вставить("Имя", "ЭлектронныйДокументооборот");
	Описание.Вставить("Представление", Нстр("ru = 'ЭДО'"));
	
	Возврат Описание;
	
КонецФункции

// Возвращает настройку автоматического создания объекта.
//
// Возвращаемое значение:
//	см. ОбменЭДОДокументооборотВызовСервера.КлючНастройкиАвтоматическогоСозданияОбъекта
//
Функция КлючНастройкиАвтоматическогоСозданияОбъекта() Экспорт
	
	Возврат ОбменЭДОДокументооборотВызовСервера.КлючНастройкиАвтоматическогоСозданияОбъекта();

КонецФункции

// Возвращает имя объекта для правил автоматического создания объекта.
//
// Параметры:
//	Вид - СправочникСсылка.ВидыДокументовЭДО - Вид электронного документа.
//
// Возвращаемое значение:
//	Строка - Имя объекта.
//
Функция ИмяОбъектаПравилаАвтоматическогоСозданияОбъекта(Вид) Экспорт
	
	Возврат СтрШаблон("%1.%2", "Документ", ВРег(Строка(Вид.УникальныйИдентификатор())));
	
КонецФункции

// Возвращает представление настройки автоматического создания объекта.
//
// Параметры:
//  Правило - СправочникСсылка.ПравилаЗагрузкиДанныхВДО, Неопределено - Правило автоматического создания объектов.
// 
// Возвращаемое значение:
//  Строка
//
Функция ПредставлениеПравилаАвтоматическогоСозданияОбъектов(Правило) Экспорт
	
	Если ЗначениеЗаполнено(Правило) Тогда
		
		Возврат Строка(Правило);
		
	Иначе
		
		Возврат НСтр("ru='Выбрать правило автоматического создания...'");
		
	КонецЕсли;
	
КонецФункции
	
#КонецОбласти

// Возвращает текст сообщения, что функционал работы с ЭДО не используется в текущей версии
// 
// Возвращаемое значение:
//  Строка
Функция ТекстСообщенияФункционалНеИспользуетсяВНовойВерсии() Экспорт
	
	Возврат НСтр("ru = 'Данный функционал работы с ЭДО не используется в текущей версии программы.'");
	
КонецФункции

// Установить видимость подсказки формата ЭДО.
// 
// Параметры:
//  Форма - ФормаКлиентскогоПриложения
//  Формат - Строка - Длина 50, формат ЭДО
//  ИмяЭлементаФормы - Строка
//  ИспользоватьЭДОВДокументообороте - Булево
//
Процедура УстановитьВидимостьПодсказкиФорматаЭДО(Форма, Формат, ИмяЭлементаФормы, ИспользоватьЭДОВДокументообороте = Истина) Экспорт
	
	Элементы = Форма.Элементы;
	Если Не ЗначениеЗаполнено(Формат) Или Не ИспользоватьЭДОВДокументообороте Тогда
		Элементы[ИмяЭлементаФормы].ОтображениеПодсказки = ОтображениеПодсказки.Нет;
	Иначе
		Элементы[ИмяЭлементаФормы].ОтображениеПодсказки = ОтображениеПодсказки.ОтображатьСправа;	
	КонецЕсли;
	
КонецПроцедуры	

// Возвращает -  состояние документа - особое ?
//
// Параметры:
//   СостояниеЭДОСсылка - ПеречислениеСсылка.СостоянияДокументовЭДО
//
// Возвращаемое значение:
//   Булево
//
Функция ЭтоОсобоеСостояние(СостояниеЭДОСсылка) Экспорт
	
	Если 
			СостояниеЭДОСсылка = ПредопределенноеЗначение("Перечисление.СостоянияДокументовЭДО.Аннулирован")
		Или СостояниеЭДОСсылка = ПредопределенноеЗначение("Перечисление.СостоянияДокументовЭДО.ОжидаетсяИсправление")
		Или СостояниеЭДОСсылка = ПредопределенноеЗначение("Перечисление.СостоянияДокументовЭДО.ОжидаетсяИзвещениеПоОтклонению")
		Или СостояниеЭДОСсылка = ПредопределенноеЗначение("Перечисление.СостоянияДокументовЭДО.ОжидаетсяПодтверждениеАннулирования")
		Или СостояниеЭДОСсылка = ПредопределенноеЗначение("Перечисление.СостоянияДокументовЭДО.ТребуетсяИзвещениеПоОтклонению")
		Или СостояниеЭДОСсылка = ПредопределенноеЗначение("Перечисление.СостоянияДокументовЭДО.ТребуетсяПодписаниеИзвещенияПоОтклонению")
		Или СостояниеЭДОСсылка = ПредопределенноеЗначение("Перечисление.СостоянияДокументовЭДО.ТребуетсяПодготовкаКОтправкеИзвещенияПоОтклонению")
		Или СостояниеЭДОСсылка = ПредопределенноеЗначение("Перечисление.СостоянияДокументовЭДО.ТребуетсяОтправкаИзвещенияПоОтклонению")
		Или СостояниеЭДОСсылка = ПредопределенноеЗначение("Перечисление.СостоянияДокументовЭДО.ТребуетсяОтправкаАннулирования")
		Или СостояниеЭДОСсылка = ПредопределенноеЗначение("Перечисление.СостоянияДокументовЭДО.ТребуетсяОтправкаОтклонения")
		Или СостояниеЭДОСсылка = ПредопределенноеЗначение("Перечисление.СостоянияДокументовЭДО.ТребуетсяПодписаниеАннулирования")
		Или СостояниеЭДОСсылка = ПредопределенноеЗначение("Перечисление.СостоянияДокументовЭДО.ТребуетсяПодписаниеОтклонения")
		Или СостояниеЭДОСсылка = ПредопределенноеЗначение("Перечисление.СостоянияДокументовЭДО.ТребуетсяПодтверждениеАннулирования")   
		Или СостояниеЭДОСсылка = ПредопределенноеЗначение("Перечисление.СостоянияДокументовЭДО.ТребуетсяУточнение")
		Тогда
		
		Возврат Истина;
		
	КонецЕсли;	
	
	Возврат Ложь;
	
КонецФункции	

// Перечисление способов обработки исходящего документа
// 
// Возвращаемое значение:
//  Структура:
// * Документ - Строка
Функция СпособыОбработкиИсходящегоДокумента() Экспорт
	
	Способы = Новый Структура;
	Способы.Вставить("Документ", "Автоматически_ДокументПредприятия");
	
	Возврат Способы;
	
КонецФункции

// Возвращает имя события создания исходящего ЭДО
// 
// Возвращаемое значение:
//  Строка
Функция ИмяСобытияСозданияИсходящегоЭДО() Экспорт
	
	Возврат "СозданИсходящийЭДОПоДокументуДО";
	
КонецФункции

#КонецОбласти

#Область СлужебныйПрограммныйИнтерфейс

// Код процедур и функций

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

// Код процедур и функций

#КонецОбласти
