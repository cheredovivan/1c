
#Область ПрограммныйИнтерфейс

Процедура УстановитьДоступностьОпции(Форма, ИмяОпции, Значение) Экспорт
	
	Если Форма.Элементы[ИмяОпции].Доступность <> Значение Тогда
		Форма.Элементы[ИмяОпции].Доступность = Значение;
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

