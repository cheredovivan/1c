////////////////////////////////////////////////////////////////////////////////
// Общий модуль для работы с виджетами.
//
////////////////////////////////////////////////////////////////////////////////

#Область ПрограммныйИнтерфейс

// Возвращает структуру значения группового показателя.
// Используется при расчете группового показателя.
// После вычисления значения группового показателя свойство "Рассчитан" устанавливается в Истина,
// а в структуру добавляются свойства.
//
// Параметры:
//  ИмяГрупповогоПоказателя - Строка.
// 
// Возвращаемое значение:
//  Структура - Значение группового показателя.
//   * Рассчитан - Булево - Групповой показатель рассчитан и структура дополнена свойствами показателя.
//
Функция ЗначениеГрупповогоПоказателя(ИмяГрупповогоПоказателя) Экспорт
	
	ЗначениеГрупповогоПоказателя = Новый Структура("Рассчитан", Ложь);
	
	Возврат ЗначениеГрупповогоПоказателя;
	
КонецФункции

// Возвращает картинку разделителя рабочего стола.
Функция РазделительРабочегоСтола() Экспорт
	
	РазделительРабочегоСтола = БиблиотекаКартинок.РазделительРабочегоСтола;
	
	Возврат РазделительРабочегоСтола;
	
КонецФункции

#КонецОбласти