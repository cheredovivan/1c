#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область СлужебныйПрограммныйИнтерфейс

Функция ОтображатьОповещениеЕстьНеобработанныеНастройкиСчетовИСубконто() Экспорт
	
	ОповещениеСкрыто = Ложь;
	ХранилищеНастроек = Получить();
	
	Если ТипЗнч(ХранилищеНастроек) = Тип("ХранилищеЗначения") Тогда
		НастройкиОповещений = ХранилищеНастроек.Получить();
		Если ТипЗнч(НастройкиОповещений) <> Тип("Структура") Тогда
		ИначеЕсли НастройкиОповещений.Свойство("СкрытьОповещениеЕстьНеобработанныеНастройкиСчетовИСубконто", ОповещениеСкрыто) Тогда
		Иначе
			ОповещениеСкрыто = Ложь;
		КонецЕсли;
	КонецЕсли;
	
	Возврат НЕ ОповещениеСкрыто;
	
КонецФункции

Функция ОтображатьОповещениеЕстьНеобработанныеНастройкиКорреспонденций() Экспорт
	
	ОповещениеСкрыто = Ложь;
	ХранилищеНастроек = Получить();
	
	Если ТипЗнч(ХранилищеНастроек) = Тип("ХранилищеЗначения") Тогда
		НастройкиОповещений = ХранилищеНастроек.Получить();
		Если ТипЗнч(НастройкиОповещений) <> Тип("Структура") Тогда
		ИначеЕсли НастройкиОповещений.Свойство("СкрытьОповещениеЕстьНеобработанныеНастройкиКорреспонденций", ОповещениеСкрыто) Тогда
		Иначе
			ОповещениеСкрыто = Ложь;
		КонецЕсли;
	КонецЕсли;
	
	Возврат НЕ ОповещениеСкрыто;
	
КонецФункции

Процедура СкрытьОповещениеЕстьНеобработанныеНастройкиСчетовИСубконто() Экспорт
	
	ХранилищеНастроек = Получить();
	
	Если ТипЗнч(ХранилищеНастроек) = Тип("ХранилищеЗначения") Тогда
		НастройкиОповещений = ХранилищеНастроек.Получить();
	Иначе
		НастройкиОповещений = Новый Структура();
	КонецЕсли;
		
	Если ТипЗнч(НастройкиОповещений) <> Тип("Структура") Тогда
		НастройкиОповещений = Новый Структура();
	КонецЕсли;
		
	НастройкиОповещений.Вставить("СкрытьОповещениеЕстьНеобработанныеНастройкиСчетовИСубконто", Истина);
	
	УстановитьПривилегированныйРежим(Истина);
	
	ХранилищеНастроек = Новый ХранилищеЗначения(НастройкиОповещений);
	Установить(ХранилищеНастроек);
	
КонецПроцедуры

Процедура СкрытьОповещениеЕстьНеобработанныеНастройкиКорреспонденций() Экспорт
	
	ХранилищеНастроек = Получить();
	
	Если ТипЗнч(ХранилищеНастроек) = Тип("ХранилищеЗначения") Тогда
		НастройкиОповещений = ХранилищеНастроек.Получить();
	Иначе
		НастройкиОповещений = Новый Структура();
	КонецЕсли;
		
	Если ТипЗнч(НастройкиОповещений) <> Тип("Структура") Тогда
		НастройкиОповещений = Новый Структура();
	КонецЕсли;
		
	НастройкиОповещений.Вставить("СкрытьОповещениеЕстьНеобработанныеНастройкиКорреспонденций", Истина);
	
	УстановитьПривилегированныйРежим(Истина);
	
	ХранилищеНастроек = Новый ХранилищеЗначения(НастройкиОповещений);
	Установить(ХранилищеНастроек);
	
КонецПроцедуры

#КонецОбласти

#КонецЕсли
