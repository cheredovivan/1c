// @strict-types

#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	КоличествоПроверяемыхДокументов = Параметры.КоличествоПроверяемыхДокументов;
	
	ТекстЗаголовка = "";
	
	Если КоличествоПроверяемыхДокументов > 0 Тогда
		ТекстЗаголовка = СтрШаблон(НСтр("ru = 'Будет выполнена проверка подписей выбранных документов (%1).
                                         |Это может занять продолжительное время.
                                         |Проверить?';
                                         |en = 'The signatures of the selected documents will be checked (%1).
                                         |This might take a long time.
                                         |Check them?'"), КоличествоПроверяемыхДокументов); 
	Иначе 
		ТекстЗаголовка = НСтр("ru = 'Будет выполнена проверка подписей ВСЕХ документов, подписанных по этой МЧД.
                               |Это может занять продолжительное время.
                               |Проверить ВСЕ?';
                               |en = 'The signatures of ALL documents signed under this machine-readable letter of authority will be checked.
                               |This might take a long time.
                               |Check ALL?'");
		
	КонецЕсли;	

	Элементы.НадписьИнформация.Заголовок = ТекстЗаголовка;
				
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура Проверить(Команда)
	
	ПараметрыЗакрытия = Новый Структура("ИгнорироватьРучныеПроверкиМЧД", ИгнорироватьРучныеПроверкиМЧД);
	Закрыть(ПараметрыЗакрытия);
	
КонецПроцедуры

#КонецОбласти

