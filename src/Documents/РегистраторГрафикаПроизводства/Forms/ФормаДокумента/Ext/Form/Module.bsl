
#Область ОбработчикиСобытийЭлементовТаблицыФормыДвиженияДоступностьВидовРабочихЦентров

&НаКлиенте
Процедура ДвиженияДоступностьВидовРабочихЦентровВыбор(Элемент, ВыбраннаяСтрока, Поле, СтандартнаяОбработка)
	
	ТекущиеДанные = Элементы.ДвиженияДоступностьВидовРабочихЦентров.ТекущиеДанные;
	Если ТекущиеДанные = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	Если Поле.Имя = "ВидРабочегоЦентра"
		И ЗначениеЗаполнено(ТекущиеДанные.ВидРабочегоЦентра) Тогда
		ПоказатьЗначение(, ТекущиеДанные.ВидРабочегоЦентра);
	ИначеЕсли Поле.Имя = "Распоряжение"
		И ЗначениеЗаполнено(ТекущиеДанные.Распоряжение) Тогда
		ПоказатьЗначение(, ТекущиеДанные.Распоряжение);
	ИначеЕсли Поле.Имя = "Этап"
		И ЗначениеЗаполнено(ТекущиеДанные.Этап) Тогда
		ПоказатьЗначение(, ТекущиеДанные.Этап);
	ИначеЕсли Поле.Имя = "Подразделение"
		И ЗначениеЗаполнено(ТекущиеДанные.Подразделение) Тогда
		ПоказатьЗначение(, ТекущиеДанные.Подразделение);
	ИначеЕсли Поле.Имя = "Смена"
		И ЗначениеЗаполнено(ТекущиеДанные.Смена) Тогда
		ПоказатьЗначение(, ТекущиеДанные.Смена);
	ИначеЕсли Поле.Имя = "ВариантНаладки"
		И ЗначениеЗаполнено(ТекущиеДанные.ВариантНаладки) Тогда
		ПоказатьЗначение(, ТекущиеДанные.ВариантНаладки);
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти